/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;
/******/
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	__webpack_require__(115);
	
	__webpack_require__(116);
	
	var _platform = __webpack_require__(114);
	
	var _platform2 = _interopRequireDefault(_platform);
	
	var _onResize = __webpack_require__(13);
	
	var _onResize2 = _interopRequireDefault(_onResize);
	
	__webpack_require__(68);
	
	__webpack_require__(71);
	
	__webpack_require__(52);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	// for mobile devices, where the navigation bar moves in and out as you scroll,
	// the css vh unit makes it weird, so they must be overriden
	window.IS_MOBILE = _platform2.default.os.family === 'Android' || _platform2.default.os.family === 'iOS' || _platform2.default.os.family === 'Windows Phone';
	if (window.IS_MOBILE) {
	  Array.prototype.forEach.call(document.querySelectorAll('.vh'), vh => {
	    function resize() {
	      vh.style.height = (window.orientation === 0 ? screen.height - 32 : screen.width - 32) + 'px';
	    }
	    (0, _onResize2.default)(resize);
	    resize();
	  });
	}

/***/ },
/* 1 */
/***/ function(module, exports) {

	/* Iterable<Symbol>
	
	  Indefinitely generates Symbols to make pseudo-private members easier to define
	*/
	module.exports = {
	  *[Symbol.iterator]() { while(true) yield Symbol(); },
	  next() { return { value: Symbol(), done: false}; }
	};


/***/ },
/* 2 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.KEYFRAME_END = exports.Input = undefined;
	
	var _enumerate = __webpack_require__(8);
	
	var _enumerate2 = _interopRequireDefault(_enumerate);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const Input = exports.Input = (0, _enumerate2.default)('Left', 'Right', 'Up', 'Down', 'Attack', 'Defend', 'Talk');
	const KEYFRAME_END = exports.KEYFRAME_END = Symbol('KEYFRAME_END');

/***/ },
/* 3 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.tree_w = exports.tree_r = exports.tree_g = exports.switch_platform = exports.spark = exports.puff = exports.player = exports.grass = exports.flower_w = exports.flower_r = exports.flower_g = exports.door = exports.butterfly = exports.bird_flying = exports.bird = undefined;
	
	var _sprite = __webpack_require__(10);
	
	var _sprite2 = _interopRequireDefault(_sprite);
	
	var _bird = __webpack_require__(99);
	
	var _bird2 = _interopRequireDefault(_bird);
	
	var _bird_flying = __webpack_require__(100);
	
	var _bird_flying2 = _interopRequireDefault(_bird_flying);
	
	var _butterfly = __webpack_require__(101);
	
	var _butterfly2 = _interopRequireDefault(_butterfly);
	
	var _door = __webpack_require__(102);
	
	var _door2 = _interopRequireDefault(_door);
	
	var _flower_g = __webpack_require__(103);
	
	var _flower_g2 = _interopRequireDefault(_flower_g);
	
	var _flower_r = __webpack_require__(104);
	
	var _flower_r2 = _interopRequireDefault(_flower_r);
	
	var _flower_w = __webpack_require__(105);
	
	var _flower_w2 = _interopRequireDefault(_flower_w);
	
	var _grass = __webpack_require__(106);
	
	var _grass2 = _interopRequireDefault(_grass);
	
	var _player = __webpack_require__(107);
	
	var _player2 = _interopRequireDefault(_player);
	
	var _puff = __webpack_require__(108);
	
	var _puff2 = _interopRequireDefault(_puff);
	
	var _spark = __webpack_require__(109);
	
	var _spark2 = _interopRequireDefault(_spark);
	
	var _switch = __webpack_require__(110);
	
	var _switch2 = _interopRequireDefault(_switch);
	
	var _tree_g = __webpack_require__(111);
	
	var _tree_g2 = _interopRequireDefault(_tree_g);
	
	var _tree_r = __webpack_require__(112);
	
	var _tree_r2 = _interopRequireDefault(_tree_r);
	
	var _tree_w = __webpack_require__(113);
	
	var _tree_w2 = _interopRequireDefault(_tree_w);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const bird = exports.bird = new _sprite2.default(_bird2.default, 4);
	const bird_flying = exports.bird_flying = new _sprite2.default(_bird_flying2.default, 4);
	const butterfly = exports.butterfly = new _sprite2.default(_butterfly2.default, 4);
	const door = exports.door = new _sprite2.default(_door2.default);
	const flower_g = exports.flower_g = new _sprite2.default(_flower_g2.default);
	const flower_r = exports.flower_r = new _sprite2.default(_flower_r2.default);
	const flower_w = exports.flower_w = new _sprite2.default(_flower_w2.default);
	const grass = exports.grass = new _sprite2.default(_grass2.default, 5);
	const player = exports.player = new _sprite2.default(_player2.default, 5);
	const puff = exports.puff = new _sprite2.default(_puff2.default, 8);
	const spark = exports.spark = new _sprite2.default(_spark2.default, 17);
	const switch_platform = exports.switch_platform = new _sprite2.default(_switch2.default, 2);
	const tree_g = exports.tree_g = new _sprite2.default(_tree_g2.default);
	const tree_r = exports.tree_r = new _sprite2.default(_tree_r2.default);
	const tree_w = exports.tree_w = new _sprite2.default(_tree_w2.default);
	
	exports.default = Promise.all([bird, bird_flying, butterfly, door, flower_g, flower_r, flower_w, grass, player, puff, spark, switch_platform, tree_g, tree_r, tree_w].map(({ loaded }) => loaded));

/***/ },
/* 4 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(9);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _game = __webpack_require__(32);
	
	var _game2 = _interopRequireDefault(_game);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = (0, _actor2.default)(_game2.default);

/***/ },
/* 5 */
/***/ function(module, exports) {

	/* (Generator([...T]) => S, any, [, ...T]) => Promise<S>
	
	  Automatically progresses Promise based generators as their values become
	  available.
	
	  Basically a wrapper around generators to make them work like async functions.
	*/
	'use strict';
	module.exports = (generator, that, ...args) => new Promise((resolve, reject) => {
	    const run = generator.call(that, ...args);
	    // run the next step of the generator
	    const pass = (args) => run.next(args);
	    const fail = (error) => run.throw(error);
	    // prepare for the next result
	    const next = ({done, value}) => {
	      if(done) { return resolve(value); }
	      value
	        .then(pass, fail)
	        .then(next, reject);
	    };
	    // start the generator
	    next(run.next());
	  });


/***/ },
/* 6 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.fork = exports.monster = exports.coal = exports.wheel = exports.grass = exports.waitor = exports.dave = undefined;
	
	var _sprite = __webpack_require__(10);
	
	var _sprite2 = _interopRequireDefault(_sprite);
	
	var _dave = __webpack_require__(85);
	
	var _dave2 = _interopRequireDefault(_dave);
	
	var _waitor = __webpack_require__(91);
	
	var _waitor2 = _interopRequireDefault(_waitor);
	
	var _grass = __webpack_require__(87);
	
	var _grass2 = _interopRequireDefault(_grass);
	
	var _wheel = __webpack_require__(92);
	
	var _wheel2 = _interopRequireDefault(_wheel);
	
	var _coal = __webpack_require__(84);
	
	var _coal2 = _interopRequireDefault(_coal);
	
	var _monster = __webpack_require__(88);
	
	var _monster2 = _interopRequireDefault(_monster);
	
	var _fork = __webpack_require__(86);
	
	var _fork2 = _interopRequireDefault(_fork);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const dave = exports.dave = new _sprite2.default(_dave2.default, 5);
	const waitor = exports.waitor = new _sprite2.default(_waitor2.default);
	const grass = exports.grass = new _sprite2.default(_grass2.default);
	const wheel = exports.wheel = new _sprite2.default(_wheel2.default);
	const coal = exports.coal = new _sprite2.default(_coal2.default);
	const monster = exports.monster = new _sprite2.default(_monster2.default, 3);
	const fork = exports.fork = new _sprite2.default(_fork2.default);
	
	exports.default = Promise.all([dave, waitor, grass, wheel, coal, monster, fork].map(({ loaded }) => loaded));

/***/ },
/* 7 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.Color = undefined;
	
	var _enumerate = __webpack_require__(8);
	
	var _enumerate2 = _interopRequireDefault(_enumerate);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const Color = exports.Color = {
	  White: 0xFFFFFF,
	  Red: 0xFF0000,
	  Green: 0x008000
	};

/***/ },
/* 8 */
/***/ function(module, exports) {

	/* (...string) => Map<string, symbol>
	
	  Generates an enum given the keys. Each item in the enum is a number
	*/
	'use strict';
	module.exports = (...keys) => {
	  if(keys.indexOf('length') !== -1) {
	    throw new Error('Cannot create an enumerator with key "length"');
	  }
	  let n = 0;
	  const e = keys.reduce((e, key) => (e[key] = n++, e), {});
	  e.length = n;
	  e[Symbol.iterator] = function*() {
	    for(let i of keys) {
	      yield e[i];
	    }
	  };
	  return e;
	}


/***/ },
/* 9 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.Actor = undefined;
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _sprite = __webpack_require__(10);
	
	var _sprite2 = _interopRequireDefault(_sprite);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [SPRITE, POSITION, GAME, ORIGIN, BLEND, SUBIMAGE] = _symbolic2.default;
	
	let Actor = exports.Actor = undefined;
	
	exports.default = game => exports.Actor = Actor = class {
	  constructor(sprite) {
	    this[GAME] = game;
	    this.sprite = sprite;
	    this.position = [0, 0, 0];
	    this.subimage = 0;
	    this.blend = 0xFFFFFF;
	    this.origin = [0, 0];
	    this.alpha = 1;
	    if (this.sprite) {
	      this.origin = [this.sprite.dimensions[0] / 2, this.sprite.dimensions[1] / 2];
	    }
	    this.scale = [1, 1];
	  }
	
	  react(input) {}
	  update() {}
	
	  destroy() {
	    const i = this.game.actors.indexOf(this);
	    this.game.actors.splice(i, 1);
	  }
	
	  draw(context) {
	    if (!this.sprite) {
	      return;
	    }
	    context.globalAlpha = this.alpha;
	    this.sprite.blend(this.blend).draw(context, this.subimage, Math.sign(this.scale[0]) * this.x - this.origin[0], Math.sign(this.scale[1]) * this.y - this.origin[1], this.sprite.dimensions[0], this.sprite.dimensions[1], ...this.scale);
	    context.globalAlpha = 1;
	  }
	
	  get x() {
	    return this[POSITION][0];
	  }
	  set x(x) {
	    this[POSITION][0] = x;
	  }
	  get y() {
	    return this[POSITION][1];
	  }
	  set y(y) {
	    this[POSITION][1] = y;
	  }
	  get z() {
	    return this[POSITION][2];
	  }
	  set z(z) {
	    this[POSITION][2] = z;
	  }
	
	  get origin() {
	    return this[ORIGIN];
	  }
	
	  set origin(origin) {
	    if (Array.isArray(origin)) {
	      this[ORIGIN] = origin;
	    } else {
	      throw new TypeError('Origin must be an array');
	    }
	  }
	
	  get position() {
	    return this[POSITION];
	  }
	
	  set position(position) {
	    if (Array.isArray(position)) {
	      this[POSITION] = position;
	      while (this[POSITION].length < 3) {
	        this[POSITION].push(0);
	      }
	    } else {
	      throw new TypeError('Position must be an array');
	    }
	  }
	
	  get blend() {
	    return this[BLEND];
	  }
	
	  set blend(color) {
	    if (typeof color === 'number') {
	      this[BLEND] = color;
	    } else if (color[0] === '#') {
	      this[BLEND] = parseInt(color.slice(1), 16);
	    } else if (color.slice(0, 3).toLowerCase() === 'rgb') {
	      this[BLEND] = color.slice(4, -1).split(',').map(x => parseInt(x, 10)).reduce((p, x) => p << 2 + x, 0);
	    }
	    if (this.sprite) {
	      this.sprite.addBlend(this.blend);
	    }
	  }
	
	  get sprite() {
	    return this[SPRITE];
	  }
	
	  set sprite(sprite) {
	    if (sprite instanceof _sprite2.default || sprite === null || sprite === undefined) {
	      this[SPRITE] = sprite;
	    } else {
	      throw new TypeError('Cannot set sprite to something that is not a Sprite');
	    }
	  }
	
	  get subimage() {
	    return this[SUBIMAGE];
	  }
	
	  set subimage(index) {
	    let limit = 1;
	    if (this.sprite) {
	      limit = this.sprite.frames.length;
	    }
	    this[SUBIMAGE] = index % limit;
	  }
	
	  get dimensions() {
	    if (this.sprite) {
	      return this.sprite.dimensions;
	    } else {
	      return [0, 0];
	    }
	  }
	
	  get game() {
	    return game;
	  }
	};

/***/ },
/* 10 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [IMAGE, DIMENSIONS, LOADED, BLENDS, SPRITE] = _symbolic2.default;
	
	function blend(canvas, color) {
	  const context = canvas.getContext('2d');
	  const img = context.getImageData(0, 0, canvas.width, canvas.height);
	  const [r, g, b] = [(color >> 16 & 0xFF) / 0xFF, (color >> 8 & 0xFF) / 0xFF, (color & 0xFF) / 0xFF];
	  for (let i = 0; i < img.data.length; i += 4) {
	    img.data[i] *= r;
	    img.data[i + 1] *= g;
	    img.data[i + 2] *= b;
	  }
	  context.clearRect(0, 0, canvas.width, canvas.height);
	  context.putImageData(img, 0, 0);
	}
	
	class Sprite {
	  constructor(imageUrl, frames = 1) {
	    if (imageUrl) {
	      this[IMAGE] = new Image();
	      this[IMAGE].src = imageUrl;
	      this[DIMENSIONS] = [0, 0];
	      this[LOADED] = new Promise((resolve, reject) => {
	        this[IMAGE].onload = () => {
	          this[DIMENSIONS] = [Math.floor(this[IMAGE].width / frames), this[IMAGE].height];
	          resolve();
	        };
	        this[IMAGE].onerror = reject;
	      });
	      this[BLENDS] = new Map([[0xFFFFFF, this]]);
	    }
	  }
	
	  get frames() {
	    if (window.Proxy) {
	      return new Proxy(this, {
	        get(target, frame) {
	          if (frame === 'length') {
	            return target[IMAGE].width / target[DIMENSIONS][0];
	          } else {
	            return [target[DIMENSIONS][0] * (frame % (target[IMAGE].width / target[DIMENSIONS][0])), 0, ...target[DIMENSIONS]];
	          }
	        }
	      });
	    } else {
	      const frames = [];
	      for (let i = 0; i < this[IMAGE].width / this[DIMENSIONS][0]; ++i) {
	        frames.push([this[DIMENSIONS][0] * (i % (this[IMAGE].width / this[DIMENSIONS][0])), 0, ...this[DIMENSIONS]]);
	      }
	      return frames;
	    }
	  }
	
	  get dimensions() {
	    return this[DIMENSIONS];
	  }
	
	  get image() {
	    return this[IMAGE];
	  }
	
	  get loaded() {
	    return this[LOADED];
	  }
	
	  addBlend(color) {
	    if (!this[BLENDS].has(color)) {
	      const canvas = document.createElement('canvas');
	      canvas.width = this[IMAGE].width;
	      canvas.height = this[IMAGE].height;
	      canvas.style.display = 'none';
	      canvas.style.width = `${ canvas.width }px`;
	      canvas.style.height = `${ canvas.height }px`;
	      const context = canvas.getContext('2d');
	      context.clearRect(0, 0, canvas.width, canvas.height);
	      context.drawImage(this[BLENDS].get(0xFFFFFF)[IMAGE], 0, 0);
	      blend(canvas, color);
	      this[BLENDS].set(color, new BlendedSprite(canvas, this[SPRITE] || this));
	    }
	  }
	
	  blend(color) {
	    return this[BLENDS].get(color);
	  }
	
	  draw(context, frame, x, y, w = this[DIMENSIONS][0], h = this[DIMENSIONS][1], sx = 1, sy = 1) {
	    context.save();
	    context.scale(...[sx, sy].map(Math.sign));
	    context.drawImage(this[IMAGE], ...this.frames[frame], Math.floor(x), Math.floor(y), Math.floor(w) * Math.abs(sx), Math.floor(h) * Math.abs(sy));
	    context.restore();
	  }
	
	  drawRotated(context, frame, x, y, angle, cx = this[DIMENSIONS][0] / 2, cy = this[DIMENSIONS][1] / 2, w = this[DIMENSIONS][0], h = this[DIMENSIONS][1], sx = 1, sy = 1) {
	    context.save();
	    context.translate(Math.floor(x), Math.floor(y));
	    context.rotate(angle * Math.PI / 180);
	    context.scale(...[sx, sy].map(Math.sign));
	    context.drawImage(this[IMAGE], ...this.frames[frame], -Math.floor(cx), -Math.floor(cy), Math.floor(w) * Math.abs(sx), Math.floor(h) * Math.abs(sy));
	    context.restore();
	  }
	}
	
	class BlendedSprite extends Sprite {
	  constructor(canvas, sprite) {
	    super(null, sprite.frames.length);
	    this[SPRITE] = sprite;
	    this[IMAGE] = canvas;
	  }
	
	  get frames() {
	    return this[SPRITE].frames;
	  }
	  get dimensions() {
	    return this[SPRITE].dimensions;
	  }
	  get loaded() {
	    return this[SPRITE].loaded;
	  }
	  get image() {
	    return this[IMAGE];
	  }
	  blend(color) {
	    return this[SPRITE].blend(color);
	  }
	  addBlend(color) {
	    return this[SPRITE].addBlend(color);
	  }
	}
	
	exports.default = Sprite;

/***/ },
/* 11 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(9);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _game = __webpack_require__(28);
	
	var _game2 = _interopRequireDefault(_game);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = (0, _actor2.default)(_game2.default);

/***/ },
/* 12 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _const = __webpack_require__(2);
	
	var _sprites = __webpack_require__(3);
	
	var _solid = __webpack_require__(21);
	
	var _solid2 = _interopRequireDefault(_solid);
	
	var _spark = __webpack_require__(34);
	
	var _spark2 = _interopRequireDefault(_spark);
	
	var _puff = __webpack_require__(63);
	
	var _puff2 = _interopRequireDefault(_puff);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const accel = 1;
	const gravity = 0.25;
	const speed = 3;
	const jump = -6;
	
	class Player extends _actor2.default {
	  constructor() {
	    super(_sprites.player);
	
	    this.haxis = 0;
	
	    this.hsp = 0;
	    this.vsp = 0;
	    this.subimage = 2;
	
	    this.position = [32, 400, 1];
	    this.origin[1] = this.sprite.dimensions[1];
	  }
	
	  react(input) {
	    switch (input) {
	      case _const.Input.Left:
	        this.haxis -= 1;
	        break;
	      case _const.Input.Right:
	        this.haxis += 1;
	        break;
	      case _const.Input.Up:
	        this.vsp = jump;
	        break;
	    }
	  }
	
	  update() {
	    if (this.haxis !== 0) {
	      if (this.hsp === 0 && this.vsp === 0) {
	        this.game.add.actor(new _puff2.default([this.position[0] - this.haxis * 8, this.position[1]], -this.haxis));
	      }
	      this.hsp += this.haxis * accel;
	    }
	    if (this.haxis !== Math.sign(this.hsp)) {
	      this.hsp -= Math.sign(this.hsp) * Math.min(Math.abs(this.hsp), accel);
	      if (Math.abs(this.hsp) < 1) {
	        this.hsp = 0;
	      }
	    }
	    this.hsp = Math.max(-speed, Math.min(this.hsp, speed));
	    const wall = this.game.util.collision.between(this).at([this.x, this.y + this.vsp + 1]).and(_solid2.default);
	    if (!wall) {
	      this.vsp = Math.max(Math.min(this.vsp + gravity, 10), -10);
	    } else {
	      this.position[1] = wall.position[1];
	      if (this.vsp !== 0) {
	        this.game.add.actor(new _puff2.default([this.position[0] - 8, this.position[1]], -1), new _puff2.default([this.position[0] + 8, this.position[1]], 1));
	        this.vsp = 0;
	      }
	    }
	    if (this.hsp || this.vsp) {
	      this.position[0] += this.hsp;
	      this.position[1] += this.vsp;
	    }
	    const grey = Math.floor(Math.sqrt(Math.pow(this.vsp, 2) + Math.pow(this.hsp, 2)) * 2) * 10;
	    this.blend = 0xFFFFFF - 0x010101 * grey;
	    this.subimage = Math.floor(Math.abs(this.hsp / (speed / 2))) * Math.sign(this.hsp) + 2;
	    const spark = this.game.util.collision.between(this).and(_spark2.default);
	    spark && spark.useColor();
	    this.haxis = 0;
	  }
	}
	exports.default = Player;

/***/ },
/* 13 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.default = onResize;
	function onResize(fn) {
	  setTimeout(() => {
	    if (window.IS_MOBILE) {
	      window.addEventListener('orientationchange', () => {
	        window.addEventListener('resize', function resize() {
	          fn();
	          window.removeEventListener(resize);
	        }, { once: true });
	      });
	    } else {
	      window.addEventListener('resize', fn);
	    }
	  }, 0);
	}

/***/ },
/* 14 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [IMAGE, POSITION, LOADED] = _symbolic2.default;
	
	class Background {
	  constructor(imageUrl, position = [0, 0, -1000000]) {
	    this.position = position;
	    this[IMAGE] = new Image();
	    this[IMAGE].src = imageUrl;
	    this[LOADED] = new Promise((resolve, reject) => {
	      this[IMAGE].onload = resolve;
	      this[IMAGE].onerror = reject;
	    });
	  }
	
	  draw(context) {
	    context.drawImage(this[IMAGE], ...this[POSITION].slice(0, 2));
	  }
	
	  get loaded() {
	    return this[LOADED];
	  }
	
	  get position() {
	    return this[POSITION];
	  }
	
	  set position(position) {
	    if (Array.isArray(position)) {
	      this[POSITION] = position;
	      while (this[POSITION].length < 3) {
	        this[POSITION].push(this[POSITION].length === 2 ? -1000000 : 0);
	      }
	    } else {
	      throw new TypeError('Position must be an array');
	    }
	  }
	
	  get dimensions() {
	    return [this[IMAGE].width, this[IMAGE].height];
	  }
	
	  copy(position = this[POSITION]) {
	    // this.constructor used to support Foregrounds default depth
	    return new this.constructor(this[IMAGE].src, position);
	  }
	}
	exports.default = Background;

/***/ },
/* 15 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _generate = __webpack_require__(5);
	
	var _generate2 = _interopRequireDefault(_generate);
	
	var _const = __webpack_require__(2);
	
	var _util = __webpack_require__(41);
	
	var _util2 = _interopRequireDefault(_util);
	
	var _onResize = __webpack_require__(13);
	
	var _onResize2 = _interopRequireDefault(_onResize);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	let playCount = 0;
	
	const [CANVAS, CONTEXT, ON, END, PAUSE, UNPAUSE, KEYFRAMES, ACTORS, BACKGROUNDS, FOREGROUNDS, PLAYING, GLOBAL] = _symbolic2.default;
	
	class Game {
	  constructor(canvas, keyframes) {
	    this[CANVAS] = canvas;
	    this[CONTEXT] = canvas.getContext('2d');
	    this[END] = false;
	    this[PAUSE] = null;
	    this[UNPAUSE] = null;
	    this[KEYFRAMES] = keyframes;
	    this[ACTORS] = [];
	    this[BACKGROUNDS] = [];
	    this[FOREGROUNDS] = [];
	    this[PLAYING] = false;
	    this[ON] = {};
	    this[GLOBAL] = {};
	    (0, _onResize2.default)(() => this.trigger('resize'));
	  }
	
	  play() {
	    playCount++;
	    let frame = 0;
	    this[PLAYING] = true;
	    this.trigger('start');
	    const step = now => {
	      if (!this[PLAYING]) {
	        return;
	      } // force quit
	      const inputs = this[KEYFRAMES].get(frame++);
	      if (this[END] || inputs.has(_const.KEYFRAME_END)) {
	        this[END] = false;
	        this[PLAYING] = false;
	        this.trigger('end');
	        return;
	      }
	      this[ACTORS] = this[ACTORS].sort(({ position: [,, a] }, { position: [,, b] }) => a - b);
	      this[CONTEXT].clearRect(0, 0, this[CANVAS].width, this[CANVAS].height);
	      for (let actor of this[ACTORS]) {
	        inputs.forEach(actor.react.bind(actor));
	      }
	      for (let actor of this[ACTORS]) {
	        actor.update();
	      }
	      this.trigger('step');
	      const drawables = [].concat(this[BACKGROUNDS], this[FOREGROUNDS], this[ACTORS]).sort(({ position: [,, a] }, { position: [,, b] }) => a - b);
	      for (let item of drawables) {
	        item.draw(this[CONTEXT]);
	      }
	      if (this[PAUSE]) {
	        (0, _generate2.default)(function* () {
	          yield this[PAUSE];
	          window.requestAnimationFrame(step);
	        }, this);
	      } else {
	        window.requestAnimationFrame(step);
	      }
	    };
	    window.requestAnimationFrame(step);
	  }
	
	  end() {
	    this[END] = true;
	    if (this[PAUSE]) {
	      this[UNPAUSE]();
	      this[PAUSE] = this[UNPAUSE] = null;
	    }
	  }
	
	  pause() {
	    if (!this[PAUSE]) {
	      this.trigger('pause');
	      this[PAUSE] = new Promise(resolve => {
	        this[UNPAUSE] = () => {
	          this[PAUSE] = null;
	          this.trigger('unpause');
	          resolve();
	        };
	      });
	    } else {
	      this[UNPAUSE]();
	    }
	  }
	
	  reset() {
	    this[ACTORS] = [];
	    this[BACKGROUNDS] = [];
	    this[FOREGROUNDS] = [];
	    this[PLAYING] = false;
	    this[END] = false;
	    if (this[PAUSE]) {
	      this[UNPAUSE]();
	      this[PAUSE] = this[UNPAUSE] = null;
	    }
	    return this;
	  }
	
	  get actors() {
	    return this[ACTORS];
	  }
	  get foregrounds() {
	    return this[FOREGROUNDS];
	  }
	  get backgrounds() {
	    return this[BACKGROUNDS];
	  }
	
	  get width() {
	    return this[CANVAS].width;
	  }
	  get height() {
	    return this[CANVAS].height;
	  }
	  get canvas() {
	    return this[CANVAS];
	  }
	  get global() {
	    return this[GLOBAL];
	  }
	
	  get add() {
	    return {
	      actor: (...actors) => {
	        this.actors.push(...actors);
	        return this;
	      },
	      foreground: (...foregrounds) => {
	        this.foregrounds.push(...foregrounds);
	        return this;
	      },
	      background: (...backgrounds) => {
	        this.backgrounds.push(...backgrounds);
	        return this;
	      }
	    };
	  }
	
	  on(evt, fn) {
	    this[ON][evt] = this[ON][evt] ? [...this[ON][evt], fn] : [fn];
	    return this;
	  }
	
	  trigger(evt) {
	    for (let f of this[ON][evt] || []) {
	      f.bind(this)();
	    }
	  }
	
	  get util() {
	    return (0, _util2.default)(this);
	  }
	}
	exports.default = Game;

/***/ },
/* 16 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _const = __webpack_require__(2);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [INDICES, FRAMES, PAIRS] = _symbolic2.default;
	
	class Keyframes {
	  constructor() {
	    this[PAIRS] = [];
	  }
	
	  frame(frame, values, next = values) {
	    frame *= 60;
	    this[PAIRS][Math.floor(frame)] = [values, next];
	    return this;
	  }
	
	  end(frame) {
	    frame *= 60;
	    this[PAIRS][Math.floor(frame)] = [[_const.KEYFRAME_END], []];
	    return this;
	  }
	
	  get(frame) {
	    frame = Math.floor(frame);
	    const keys = Object.keys(this[PAIRS]).map(x => +x);
	    let values = [];
	    for (let key of keys) {
	      if (key >= frame) {
	        if (key === frame) {
	          values = this[PAIRS][key];
	          values = values[0].concat(values[1]);
	        }
	        break;
	      }
	      values = this[PAIRS][key][1];
	    }
	    return new Set(values);
	  }
	}
	exports.default = Keyframes;

/***/ },
/* 17 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.spring = exports.souvenir = exports.purple = exports.green = exports.blue = undefined;
	
	var _sprite = __webpack_require__(10);
	
	var _sprite2 = _interopRequireDefault(_sprite);
	
	var _blue_player = __webpack_require__(93);
	
	var _blue_player2 = _interopRequireDefault(_blue_player);
	
	var _green_player = __webpack_require__(95);
	
	var _green_player2 = _interopRequireDefault(_green_player);
	
	var _purple_player = __webpack_require__(96);
	
	var _purple_player2 = _interopRequireDefault(_purple_player);
	
	var _souvenir = __webpack_require__(97);
	
	var _souvenir2 = _interopRequireDefault(_souvenir);
	
	var _spring = __webpack_require__(98);
	
	var _spring2 = _interopRequireDefault(_spring);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const blue = exports.blue = new _sprite2.default(_blue_player2.default);
	const green = exports.green = new _sprite2.default(_green_player2.default);
	const purple = exports.purple = new _sprite2.default(_purple_player2.default);
	const souvenir = exports.souvenir = new _sprite2.default(_souvenir2.default);
	const spring = exports.spring = new _sprite2.default(_spring2.default);
	
	exports.default = Promise.all([].map(({ loaded }) => loaded));

/***/ },
/* 18 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _background = __webpack_require__(14);
	
	var _background2 = _interopRequireDefault(_background);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	class Foreground extends _background2.default {
	  constructor(imageUrl, position = [0, 0, 1000000]) {
	    while (position.length < 3) {
	      position.push(position.length === 2 ? 1000000 : 0);
	    }
	    super(imageUrl, position);
	  }
	
	  get position() {
	    return super.position;
	  }
	
	  set position(position) {
	    if (Array.isArray(position)) {
	      while (position.length < 3) {
	        position.push(position.length === 2 ? 1000000 : 0);
	      }
	      super.position = position;
	    } else {
	      throw new TypeError('Position must be an array');
	    }
	  }
	}
	exports.default = Foreground;

/***/ },
/* 19 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.monster = exports.elliot = exports.abby = undefined;
	
	var _sprite = __webpack_require__(10);
	
	var _sprite2 = _interopRequireDefault(_sprite);
	
	var _abby = __webpack_require__(76);
	
	var _abby2 = _interopRequireDefault(_abby);
	
	var _elliot = __webpack_require__(78);
	
	var _elliot2 = _interopRequireDefault(_elliot);
	
	var _monster = __webpack_require__(81);
	
	var _monster2 = _interopRequireDefault(_monster);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const abby = exports.abby = new _sprite2.default(_abby2.default, 4);
	const elliot = exports.elliot = new _sprite2.default(_elliot2.default, 4);
	const monster = exports.monster = new _sprite2.default(_monster2.default, 4);
	
	exports.default = Promise.all([abby, elliot, monster].map(({ loaded }) => loaded));

/***/ },
/* 20 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.Special = undefined;
	
	var _const = __webpack_require__(2);
	
	var _keyframes = __webpack_require__(16);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const Special = exports.Special = {
	  Transform: Symbol(),
	  Die: Symbol(),
	  Attack: Symbol()
	};
	
	exports.default = new _keyframes2.default().frame(0, [_const.Input.Right]).frame(2, [Special.Transform], []).frame(3, [_const.Input.Attack], []).frame(3.6, [_const.Input.Attack], []).frame(4.2, [Special.Attack], [_const.Input.Defend]).frame(5.2, [_const.Input.Attack], []).frame(5.6, [_const.Input.Attack, Special.Die], []).frame(6.1, [_const.Input.Right]).end(10);

/***/ },
/* 21 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	class Solid extends _actor2.default {}exports.default = Solid;
	;

/***/ },
/* 22 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	const phone = exports.phone = window.matchMedia('(max-width: 640px)');
	const tablet = exports.tablet = window.matchMedia('(min-width: 641px) and (max-width: 960px)');
	const desktop = exports.desktop = window.matchMedia('(min-width: 961px)');

/***/ },
/* 23 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/b5c5ab66d8331513696fe3ec992187a0.png";

/***/ },
/* 24 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(9);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _game = __webpack_require__(25);
	
	var _game2 = _interopRequireDefault(_game);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = (0, _actor2.default)(_game2.default);

/***/ },
/* 25 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _gameEngine = __webpack_require__(15);
	
	var _gameEngine2 = _interopRequireDefault(_gameEngine);
	
	var _keyframes = __webpack_require__(26);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const canvas = document.querySelector('#abby-and-elliot');
	canvas.width = 1800;
	canvas.height = 640;
	setTimeout(() => canvas.style.width = (window.IS_MOBILE ? screen.width : window.innerWidth) + 'px', 0);
	
	exports.default = new _gameEngine2.default(canvas, _keyframes2.default);

/***/ },
/* 26 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.Special = undefined;
	
	var _const = __webpack_require__(2);
	
	var _keyframes = __webpack_require__(16);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const Special = exports.Special = {
	  Monster: Symbol()
	};
	
	exports.default = new _keyframes2.default().frame(0, [_const.Input.Right]).frame(4, [_const.Input.Talk]).frame(5, [Special.Monster], [_const.Input.Talk]).frame(6, []).frame(6.5, [_const.Input.Left]).end(10);

/***/ },
/* 27 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _enumerate = __webpack_require__(8);
	
	var _enumerate2 = _interopRequireDefault(_enumerate);
	
	var _actor = __webpack_require__(11);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _const = __webpack_require__(2);
	
	var _sprites = __webpack_require__(6);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const Subimage = (0, _enumerate2.default)('Idle', 'Walk1', 'Walk2', 'Attack', 'Defend');
	
	const speed = 6;
	const imageSpeed = 60 / 8;
	const attackTime = 14;
	
	class Dave extends _actor2.default {
	  constructor() {
	    super(_sprites.dave);
	    this.attacking = 0;
	    this.defending = false;
	
	    this.hsp = 0;
	    this.vsp = 0;
	
	    this.animationTimer = imageSpeed;
	    this.walkFrame = 0;
	
	    this.position = [0, 470];
	  }
	
	  react(input) {
	    switch (input) {
	      case _const.Input.Right:
	        this.hsp = speed;
	        break;
	      case _const.Input.Attack:
	        if (!this.attacking) {
	          this.attacking = attackTime;
	        }
	        break;
	      case _const.Input.Defend:
	        this.defending = true;
	        this.attacking = 0;
	        break;
	    }
	  }
	
	  update() {
	    if (this.attacking) {
	      if (this.attacking > 2) {
	        this.subimage = Subimage.Attack;
	      } else {
	        this.subimage = Subimage.Idle;
	      }
	      --this.attacking;
	    } else if (this.defending) {
	      this.subimage = Subimage.Defend;
	    } else if (this.hsp || this.vsp) {
	      this.position[0] += this.hsp;
	      this.position[1] += this.vsp;
	      this.subimage = 1 + this.walkFrame;
	      if (!Math.floor(--this.animationTimer)) {
	        this.walkFrame = 1 - this.walkFrame;
	        this.animationTimer += imageSpeed;
	      }
	      this.scale[0] = Math.sign(this.hsp) || this.scale[0];
	    } else {
	      this.subimage = Subimage.Idle;
	    }
	    this.hsp = this.vsp = 0;
	    this.defending = false;
	  }
	
	  draw(context) {
	    if (this.attacking > 2) {
	      if (this.scale[0] === 1) {
	        const forkPos = [this.position[0] + 112, this.position[1] + 9];
	        _sprites.fork.drawRotated(context, 0, ...forkPos, 90);
	      } else {
	        const forkPos = [this.position[0] - 112, this.position[1] + 9];
	        _sprites.fork.drawRotated(context, 0, ...forkPos, 270);
	      }
	    }
	    super.draw(context);
	  }
	}
	exports.default = Dave;

/***/ },
/* 28 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _gameEngine = __webpack_require__(15);
	
	var _gameEngine2 = _interopRequireDefault(_gameEngine);
	
	var _keyframes = __webpack_require__(20);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const canvas = document.querySelector('#forks');
	canvas.width = 1800;
	canvas.height = 800;
	setTimeout(() => canvas.style.width = (window.IS_MOBILE ? screen.width : window.innerWidth) + 'px', 0);
	
	exports.default = new _gameEngine2.default(canvas, _keyframes2.default);

/***/ },
/* 29 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _generate = __webpack_require__(5);
	
	var _generate2 = _interopRequireDefault(_generate);
	
	var _sprites = __webpack_require__(6);
	
	var _sprites2 = _interopRequireDefault(_sprites);
	
	var _backgrounds = __webpack_require__(46);
	
	var _backgrounds2 = _interopRequireDefault(_backgrounds);
	
	var _game = __webpack_require__(28);
	
	var _game2 = _interopRequireDefault(_game);
	
	var _grass = __webpack_require__(48);
	
	var _grass2 = _interopRequireDefault(_grass);
	
	var _dave = __webpack_require__(27);
	
	var _dave2 = _interopRequireDefault(_dave);
	
	var _waitor = __webpack_require__(50);
	
	var _waitor2 = _interopRequireDefault(_waitor);
	
	var _wheel = __webpack_require__(51);
	
	var _wheel2 = _interopRequireDefault(_wheel);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	(0, _generate2.default)(function* () {
	  yield _sprites2.default;
	  yield _backgrounds2.default;
	  _game2.default.on('start', () => {
	    _game2.default.add.actor(new _grass2.default(), new _dave2.default(), new _waitor2.default(), new _wheel2.default([224, 704]), new _wheel2.default([1344, 704]), new _wheel2.default([1814, 709])).add.background(_backgrounds.train, _backgrounds.train.copy([1590, 5])).add.foreground(_backgrounds.rails, _backgrounds.rails.copy([1590, 5]));
	  }).on('end', () => {
	    _game2.default.reset().play();
	  }).on('resize', () => {
	    _game2.default.canvas.style.width = window.innerWidth + 'px';
	  }).play();
	});

/***/ },
/* 30 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(9);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _game = __webpack_require__(31);
	
	var _game2 = _interopRequireDefault(_game);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = (0, _actor2.default)(_game2.default);

/***/ },
/* 31 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _gameEngine = __webpack_require__(15);
	
	var _gameEngine2 = _interopRequireDefault(_gameEngine);
	
	var _keyframes = __webpack_require__(56);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const canvas = document.querySelector('#tokaido');
	canvas.width = 1800;
	canvas.height = 570;
	setTimeout(() => canvas.style.width = (window.IS_MOBILE ? screen.width : window.innerWidth) + 'px', 0);
	
	exports.default = new _gameEngine2.default(canvas, _keyframes2.default);

/***/ },
/* 32 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _gameEngine = __webpack_require__(15);
	
	var _gameEngine2 = _interopRequireDefault(_gameEngine);
	
	var _keyframes = __webpack_require__(33);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const canvas = document.querySelector('#white');
	canvas.width = 1800;
	canvas.height = 480;
	setTimeout(() => canvas.style.width = (window.IS_MOBILE ? screen.width : window.innerWidth) + 'px', 0);
	
	exports.default = new _gameEngine2.default(canvas, _keyframes2.default);

/***/ },
/* 33 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.Special = undefined;
	
	var _const = __webpack_require__(2);
	
	var _keyframes = __webpack_require__(16);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const Special = exports.Special = {
	  Elevator: Symbol()
	};
	
	exports.default = new _keyframes2.default().frame(1, [_const.Input.Right]).frame(1.8, []).frame(2.5, [_const.Input.Up], [_const.Input.Right]).frame(6.3, []).frame(6.5, [Special.Elevator]).frame(8, [_const.Input.Right, Special.Elevator]).frame(8.5, [_const.Input.Right]).frame(11.05, []).end(11.5);

/***/ },
/* 34 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _pad = __webpack_require__(39);
	
	var _pad2 = _interopRequireDefault(_pad);
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(3);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const imageSpeed = 4;
	
	class Spark extends _actor2.default {
	  constructor(position, color) {
	    super(_sprites.spark);
	    this.blend = this.color = color;
	    this.position = position;
	    this.imageTimer = imageSpeed;
	  }
	
	  update() {
	    if (! --this.imageTimer) {
	      ++this.subimage;
	      this.imageTimer = imageSpeed;
	    }
	  }
	
	  useColor() {
	    this.game.global.colors.add(this.color);
	    this.game.canvas.style.backgroundColor = `#${ (0, _pad2.default)(this.color.toString(16), '0', 6) }`;
	  }
	}
	exports.default = Spark;

/***/ },
/* 35 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.createElement = createElement;
	exports.setAttribute = setAttribute;
	exports.getAttribute = getAttribute;
	exports.animate = animate;
	function createElement(name) {
	  return document.createElementNS('http://www.w3.org/2000/svg', name);
	}
	
	function setAttribute(element, name, value) {
	  return element.setAttributeNS(null, name, value);
	}
	
	function getAttribute(element, name) {
	  return element.getAttributeNS(null, name);
	}
	
	function animate(path, time) {
	  // this is only approximate... for more complex designs use multiple paths
	  const segments = (getAttribute(path, 'd').match(/m/ig) || []).length;
	  const len = path.getTotalLength() / segments;
	  let finish, cancel;
	  const finished = new Promise((resolve, reject) => {
	    finish = resolve;
	    cancel = reject;
	  });
	  let start;
	  let animation = window.requestAnimationFrame(function animate(now) {
	    if (start === undefined) {
	      start = now;
	    } else {
	      const newlen = len * Math.pow(Math.sin(Math.max(0, (time + start - now) / time) * Math.PI / 2), 2);
	      setAttribute(path, 'stroke-dasharray', `${ len - newlen } ${ newlen }`);
	      setAttribute(path, 'stroke-dashoffset', len - newlen);
	    }
	    if (now - start < time) {
	      animation = window.requestAnimationFrame(animate);
	    } else {
	      finish(true);
	    }
	  });
	  finished.cancel = () => {
	    window.cancelAnimationFrame(animation);
	    cancel();
	  };
	  finished.skip = () => {
	    window.cancelAnimationFrame(animation);
	    setAttribute(path, 'stroke-dasharray', `${ len }`);
	    setAttribute(path, 'stroke-dashoffset', 0);
	    finish(false);
	  };
	  return finished;
	};

/***/ },
/* 36 */
/***/ function(module, exports) {

	/*
		MIT License http://www.opensource.org/licenses/mit-license.php
		Author Tobias Koppers @sokra
	*/
	// css base code, injected by the css-loader
	module.exports = function() {
		var list = [];
	
		// return the list of modules as css string
		list.toString = function toString() {
			var result = [];
			for(var i = 0; i < this.length; i++) {
				var item = this[i];
				if(item[2]) {
					result.push("@media " + item[2] + "{" + item[1] + "}");
				} else {
					result.push(item[1]);
				}
			}
			return result.join("");
		};
	
		// import a list of modules into the list
		list.i = function(modules, mediaQuery) {
			if(typeof modules === "string")
				modules = [[null, modules, ""]];
			var alreadyImportedModules = {};
			for(var i = 0; i < this.length; i++) {
				var id = this[i][0];
				if(typeof id === "number")
					alreadyImportedModules[id] = true;
			}
			for(i = 0; i < modules.length; i++) {
				var item = modules[i];
				// skip already imported module
				// this implementation is not 100% perfect for weird media query combinations
				//  when a module is imported multiple times with different media queries.
				//  I hope this will never occur (Hey this way we have smaller bundles)
				if(typeof item[0] !== "number" || !alreadyImportedModules[item[0]]) {
					if(mediaQuery && !item[2]) {
						item[2] = mediaQuery;
					} else if(mediaQuery) {
						item[2] = "(" + item[2] + ") and (" + mediaQuery + ")";
					}
					list.push(item);
				}
			}
		};
		return list;
	};


/***/ },
/* 37 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/41c88a84191b17c714600b9c84e79f81.jpg";

/***/ },
/* 38 */
/***/ function(module, exports, __webpack_require__) {

	/*
		MIT License http://www.opensource.org/licenses/mit-license.php
		Author Tobias Koppers @sokra
	*/
	var stylesInDom = {},
		memoize = function(fn) {
			var memo;
			return function () {
				if (typeof memo === "undefined") memo = fn.apply(this, arguments);
				return memo;
			};
		},
		isOldIE = memoize(function() {
			return /msie [6-9]\b/.test(window.navigator.userAgent.toLowerCase());
		}),
		getHeadElement = memoize(function () {
			return document.head || document.getElementsByTagName("head")[0];
		}),
		singletonElement = null,
		singletonCounter = 0,
		styleElementsInsertedAtTop = [];
	
	module.exports = function(list, options) {
		if(false) {
			if(typeof document !== "object") throw new Error("The style-loader cannot be used in a non-browser environment");
		}
	
		options = options || {};
		// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
		// tags it will allow on a page
		if (typeof options.singleton === "undefined") options.singleton = isOldIE();
	
		// By default, add <style> tags to the bottom of <head>.
		if (typeof options.insertAt === "undefined") options.insertAt = "bottom";
	
		var styles = listToStyles(list);
		addStylesToDom(styles, options);
	
		return function update(newList) {
			var mayRemove = [];
			for(var i = 0; i < styles.length; i++) {
				var item = styles[i];
				var domStyle = stylesInDom[item.id];
				domStyle.refs--;
				mayRemove.push(domStyle);
			}
			if(newList) {
				var newStyles = listToStyles(newList);
				addStylesToDom(newStyles, options);
			}
			for(var i = 0; i < mayRemove.length; i++) {
				var domStyle = mayRemove[i];
				if(domStyle.refs === 0) {
					for(var j = 0; j < domStyle.parts.length; j++)
						domStyle.parts[j]();
					delete stylesInDom[domStyle.id];
				}
			}
		};
	}
	
	function addStylesToDom(styles, options) {
		for(var i = 0; i < styles.length; i++) {
			var item = styles[i];
			var domStyle = stylesInDom[item.id];
			if(domStyle) {
				domStyle.refs++;
				for(var j = 0; j < domStyle.parts.length; j++) {
					domStyle.parts[j](item.parts[j]);
				}
				for(; j < item.parts.length; j++) {
					domStyle.parts.push(addStyle(item.parts[j], options));
				}
			} else {
				var parts = [];
				for(var j = 0; j < item.parts.length; j++) {
					parts.push(addStyle(item.parts[j], options));
				}
				stylesInDom[item.id] = {id: item.id, refs: 1, parts: parts};
			}
		}
	}
	
	function listToStyles(list) {
		var styles = [];
		var newStyles = {};
		for(var i = 0; i < list.length; i++) {
			var item = list[i];
			var id = item[0];
			var css = item[1];
			var media = item[2];
			var sourceMap = item[3];
			var part = {css: css, media: media, sourceMap: sourceMap};
			if(!newStyles[id])
				styles.push(newStyles[id] = {id: id, parts: [part]});
			else
				newStyles[id].parts.push(part);
		}
		return styles;
	}
	
	function insertStyleElement(options, styleElement) {
		var head = getHeadElement();
		var lastStyleElementInsertedAtTop = styleElementsInsertedAtTop[styleElementsInsertedAtTop.length - 1];
		if (options.insertAt === "top") {
			if(!lastStyleElementInsertedAtTop) {
				head.insertBefore(styleElement, head.firstChild);
			} else if(lastStyleElementInsertedAtTop.nextSibling) {
				head.insertBefore(styleElement, lastStyleElementInsertedAtTop.nextSibling);
			} else {
				head.appendChild(styleElement);
			}
			styleElementsInsertedAtTop.push(styleElement);
		} else if (options.insertAt === "bottom") {
			head.appendChild(styleElement);
		} else {
			throw new Error("Invalid value for parameter 'insertAt'. Must be 'top' or 'bottom'.");
		}
	}
	
	function removeStyleElement(styleElement) {
		styleElement.parentNode.removeChild(styleElement);
		var idx = styleElementsInsertedAtTop.indexOf(styleElement);
		if(idx >= 0) {
			styleElementsInsertedAtTop.splice(idx, 1);
		}
	}
	
	function createStyleElement(options) {
		var styleElement = document.createElement("style");
		styleElement.type = "text/css";
		insertStyleElement(options, styleElement);
		return styleElement;
	}
	
	function createLinkElement(options) {
		var linkElement = document.createElement("link");
		linkElement.rel = "stylesheet";
		insertStyleElement(options, linkElement);
		return linkElement;
	}
	
	function addStyle(obj, options) {
		var styleElement, update, remove;
	
		if (options.singleton) {
			var styleIndex = singletonCounter++;
			styleElement = singletonElement || (singletonElement = createStyleElement(options));
			update = applyToSingletonTag.bind(null, styleElement, styleIndex, false);
			remove = applyToSingletonTag.bind(null, styleElement, styleIndex, true);
		} else if(obj.sourceMap &&
			typeof URL === "function" &&
			typeof URL.createObjectURL === "function" &&
			typeof URL.revokeObjectURL === "function" &&
			typeof Blob === "function" &&
			typeof btoa === "function") {
			styleElement = createLinkElement(options);
			update = updateLink.bind(null, styleElement);
			remove = function() {
				removeStyleElement(styleElement);
				if(styleElement.href)
					URL.revokeObjectURL(styleElement.href);
			};
		} else {
			styleElement = createStyleElement(options);
			update = applyToTag.bind(null, styleElement);
			remove = function() {
				removeStyleElement(styleElement);
			};
		}
	
		update(obj);
	
		return function updateStyle(newObj) {
			if(newObj) {
				if(newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap)
					return;
				update(obj = newObj);
			} else {
				remove();
			}
		};
	}
	
	var replaceText = (function () {
		var textStore = [];
	
		return function (index, replacement) {
			textStore[index] = replacement;
			return textStore.filter(Boolean).join('\n');
		};
	})();
	
	function applyToSingletonTag(styleElement, index, remove, obj) {
		var css = remove ? "" : obj.css;
	
		if (styleElement.styleSheet) {
			styleElement.styleSheet.cssText = replaceText(index, css);
		} else {
			var cssNode = document.createTextNode(css);
			var childNodes = styleElement.childNodes;
			if (childNodes[index]) styleElement.removeChild(childNodes[index]);
			if (childNodes.length) {
				styleElement.insertBefore(cssNode, childNodes[index]);
			} else {
				styleElement.appendChild(cssNode);
			}
		}
	}
	
	function applyToTag(styleElement, obj) {
		var css = obj.css;
		var media = obj.media;
	
		if(media) {
			styleElement.setAttribute("media", media)
		}
	
		if(styleElement.styleSheet) {
			styleElement.styleSheet.cssText = css;
		} else {
			while(styleElement.firstChild) {
				styleElement.removeChild(styleElement.firstChild);
			}
			styleElement.appendChild(document.createTextNode(css));
		}
	}
	
	function updateLink(linkElement, obj) {
		var css = obj.css;
		var sourceMap = obj.sourceMap;
	
		if(sourceMap) {
			// http://stackoverflow.com/a/26603875
			css += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + " */";
		}
	
		var blob = new Blob([css], { type: "text/css" });
	
		var oldSrc = linkElement.href;
	
		linkElement.href = URL.createObjectURL(blob);
	
		if(oldSrc)
			URL.revokeObjectURL(oldSrc);
	}


/***/ },
/* 39 */
/***/ function(module, exports) {

	/* (string, string, number[, boolean]) => string
	
	  Pads str with char until it has reached the required len. Optionally can start
	  from the end by specifying end to be true.
	*/
	'use strict';
	module.exports = (str, char, len, end = false) => {
	  while(str.length < len) {
	    if(end) {
	      str += char[0];
	    } else {
	      str = char[0] + str;
	    }
	  }
	  return str;
	};


/***/ },
/* 40 */
/***/ function(module, exports) {

	/* (number) => Promise<void> & { cancel(): this, skip(): this }
	
	  Returns a promise that resolves after a timed delay. This delay is cancellable
	  and skippable by use of the wait.skip and wait.cancel
	*/
	module.exports = time => {
	  let timeout, res, rej;
	  const pr = new Promise((resolve, reject) => {
	    timeout = setTimeout(resolve, time);
	    res = resolve;
	    rej = reject;
	  });
	  pr.cancel = () => {
	    clearTimeout(timeout);
	    rej();
	    return pr;
	  };
	  pr.skip = () => {
	    clearTimeout(timeout);
	    res();
	    return pr;
	  };
	  return pr;
	}


/***/ },
/* 41 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.default = Util;
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [GAME, EXECUTE, METHOD, TARGET, FLAGS, DISTANCE, DIRECTION, RANDOM, COLLISION, NEAREST, AT, TO_POSITION] = _symbolic2.default;
	
	function Util(game) {
	  let [method, target, flags, at] = ['', null, new Set(), null];
	  const util = function (...args) {
	    return method(...args);
	  };
	  Object.defineProperties(util, {
	    to: { get() {
	        return util;
	      } },
	    and: { get() {
	        return util;
	      } },
	    distance: { get() {
	        return method = distance, util;
	      } },
	    direction: { get() {
	        return method = direction, util;
	      } },
	    random: { get() {
	        return method = random, util;
	      } },
	    collision: { get() {
	        return method = collision, util;
	      } },
	    integer: { get() {
	        return flags.add('integer'), util;
	      } },
	    at: { value(where) {
	        return at = where, flags.add('at'), util;
	      } },
	    between: { value(what) {
	        return target = what, util;
	      } },
	    from: { value(what) {
	        return target = what, util;
	      } }
	  });
	
	  function toPosition(inst) {
	    switch (typeof inst) {
	      case 'function':
	        inst = game.actors.find(x => x instanceof inst).position;
	        break;
	      case 'object':
	        if (inst.position) {
	          inst = inst.position;
	        }
	        break;
	    }
	    return inst;
	  }
	
	  function distance(inst) {
	    target = toPosition(target);
	    inst = toPosition(inst);
	    return Math.sqrt(Math.pow(target[0] - inst[0], 2) + Math.pow(target[1] - inst[1], 2));
	  }
	
	  function direction(inst) {
	    target = toPosition(target);
	    inst = toPosition(inst);
	    return Math.atan2(inst[1] - target[1], inst[0] - target[0]);
	  }
	
	  function random(max) {
	    const number = Math.random() * (max - target) + target;
	    if (flags.has('integer')) {
	      return Math.floor(number);
	    }
	    return number;
	  }
	
	  function collision(what) {
	    const targets = [[], []];
	    switch (typeof target) {
	      case 'function':
	        targets[0] = game.actors.filter(a => a instanceof target);
	        break;
	      case 'object':
	        targets[0].push(target);
	        break;
	    }
	    switch (typeof what) {
	      case 'function':
	        targets[1] = game.actors.filter(a => a instanceof what);
	        break;
	      case 'object':
	        targets[1].push(what);
	        break;
	    }
	    for (let a of targets[0]) {
	      for (let b of targets[1]) {
	        let { position: pos_a, origin: ori_a, dimensions: dim_a } = a;
	        let { position: pos_b, origin: ori_b, dimensions: dim_b } = b;
	        if (flags.has('at') && at) {
	          pos_a = at;
	        }
	        pos_a = [pos_a[0] - ori_a[0], pos_a[1] - ori_a[1]];
	        pos_b = [pos_b[0] - ori_b[0], pos_b[1] - ori_b[1]];
	        const h = pos_a[0] < pos_b[0] + dim_b[0] && pos_a[0] + dim_a[0] > pos_b[0];
	        const v = pos_a[1] < pos_b[1] + dim_b[1] && pos_a[1] + dim_a[1] > pos_b[1];
	        if (h && v) {
	          return b;
	        }
	      }
	    }
	    return false;
	  }
	
	  return util;
	}

/***/ },
/* 42 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(24);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _const = __webpack_require__(2);
	
	var _sprites = __webpack_require__(19);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const speed = 3;
	const imageSpeed = 8;
	const followDistance = 120;
	
	class Abby extends _actor2.default {
	  constructor() {
	    super(_sprites.abby);
	
	    this.hsp = 0;
	    this.vsp = 0;
	
	    this.animationTimer = imageSpeed;
	    this.walkFrame = 0;
	    this.talking = false;
	
	    this.position = [200, 320];
	  }
	
	  react(input) {
	    switch (input) {
	      case _const.Input.Left:
	        this.hsp = -speed * 2;
	        break;
	      case _const.Input.Right:
	        this.hsp = speed;
	        break;
	      case _const.Input.Talk:
	        this.talking = true;
	        break;
	    }
	  }
	
	  update() {
	    if (this.hsp || this.vsp) {
	      this.position[0] += this.hsp;
	      this.position[1] += this.vsp;
	      if (!Math.floor(--this.animationTimer)) {
	        this.subimage = (this.subimage + 1) % 4;
	        this.animationTimer += imageSpeed;
	      }
	      this.scale[0] = Math.sign(this.hsp) || this.scale[0];
	    } else {
	      this.subimage = 0;
	      if (this.talking) {
	        this.scale[0] = -1;
	      } else {
	        this.scale[0] = 1;
	      }
	    }
	    this.hsp = this.vsp = 0;
	  }
	
	  draw(context) {
	    _sprites.elliot.draw(context, this.subimage, Math.sign(this.talking ? 1 : this.scale[0]) * (this.position[0] - followDistance) - _sprites.elliot.dimensions[0] / 2, Math.sign(this.scale[1]) * this.position[1] - _sprites.elliot.dimensions[1] / 2, _sprites.elliot.dimensions[0], _sprites.elliot.dimensions[1], this.talking ? 1 : this.scale[0], this.scale[1]);
	    this.talking = false;
	    super.draw(context);
	  }
	}
	exports.default = Abby;

/***/ },
/* 43 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.flower = exports.grass = exports.tree2 = exports.tree1 = exports.bush = undefined;
	
	var _background = __webpack_require__(14);
	
	var _background2 = _interopRequireDefault(_background);
	
	var _foreground = __webpack_require__(18);
	
	var _foreground2 = _interopRequireDefault(_foreground);
	
	var _bush = __webpack_require__(77);
	
	var _bush2 = _interopRequireDefault(_bush);
	
	var _tree_ = __webpack_require__(82);
	
	var _tree_2 = _interopRequireDefault(_tree_);
	
	var _tree_3 = __webpack_require__(83);
	
	var _tree_4 = _interopRequireDefault(_tree_3);
	
	var _grass = __webpack_require__(80);
	
	var _grass2 = _interopRequireDefault(_grass);
	
	var _flowers = __webpack_require__(79);
	
	var _flowers2 = _interopRequireDefault(_flowers);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const bush = exports.bush = new _background2.default(_bush2.default);
	const tree1 = exports.tree1 = new _background2.default(_tree_2.default);
	const tree2 = exports.tree2 = new _background2.default(_tree_4.default);
	const grass = exports.grass = new _background2.default(_grass2.default);
	const flower = exports.flower = new _background2.default(_flowers2.default);
	
	exports.default = Promise.all([bush, tree1, tree2, grass, flower].map(({ loaded }) => loaded));

/***/ },
/* 44 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _generate = __webpack_require__(5);
	
	var _generate2 = _interopRequireDefault(_generate);
	
	var _sprites = __webpack_require__(19);
	
	var _sprites2 = _interopRequireDefault(_sprites);
	
	var _backgrounds = __webpack_require__(43);
	
	var _backgrounds2 = _interopRequireDefault(_backgrounds);
	
	var _game = __webpack_require__(25);
	
	var _game2 = _interopRequireDefault(_game);
	
	var _abby = __webpack_require__(42);
	
	var _abby2 = _interopRequireDefault(_abby);
	
	var _monster = __webpack_require__(45);
	
	var _monster2 = _interopRequireDefault(_monster);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	(0, _generate2.default)(function* () {
	  yield _sprites2.default;
	  yield _backgrounds2.default;
	
	  const [grasses, trees, flowers] = [[], [], []];
	  const front = [];
	
	  for (let i = 0; i < _game2.default.width; i += _backgrounds.grass.dimensions[0]) {
	    for (let j = -100; j < _backgrounds.grass.dimensions[0]; j += _game2.default.util.random.integer.from(60).to(80)) {
	      const tree = [_backgrounds.tree1, _backgrounds.tree2][_game2.default.util.random.integer.from(0).to(2)];
	      const y = _game2.default.util.random.integer.from(-220).to(20);
	      trees.push(tree.copy([i + j, y, y - 10000]));
	      if (_game2.default.util.random.integer.from(0).to(2) === 0) {
	        front.push(tree.copy([i + j, y + 620, y + 10000]));
	      }
	    }
	    for (let j = 0; j < _game2.default.height; j += _backgrounds.grass.dimensions[1]) {
	      grasses.push(_backgrounds.grass.copy([i, j]));
	      flowers.push(_backgrounds.flower.copy([i + _game2.default.util.random.integer.from(0).to(_backgrounds.grass.dimensions[0]), j + _game2.default.util.random.integer.from(0).to(_backgrounds.grass.dimensions[1])]));
	    }
	  }
	  const bushx = _game2.default.util.random.integer.from(200).to(_game2.default.width - 200);
	  _game2.default.on('start', () => {
	    _game2.default.add.actor(new _abby2.default(), new _monster2.default()).add.background(...grasses, ...trees, ...flowers, _backgrounds.bush.copy([bushx, 220, -1000])).add.foreground(...front);
	  }).on('end', () => {
	    _game2.default.reset().play();
	  }).on('resize', () => {
	    _game2.default.canvas.style.width = window.innerWidth + 'px';
	  }).play();
	});

/***/ },
/* 45 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _enumerate = __webpack_require__(8);
	
	var _enumerate2 = _interopRequireDefault(_enumerate);
	
	var _actor = __webpack_require__(24);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _const = __webpack_require__(2);
	
	var _keyframes = __webpack_require__(26);
	
	var _sprites = __webpack_require__(19);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const speed = 3;
	const imageSpeed = 8;
	
	class Monster extends _actor2.default {
	  constructor(position) {
	    super(_sprites.monster);
	
	    this.hsp = 0;
	    this.vsp = 0;
	
	    this.animationTimer = imageSpeed;
	    this.walkFrame = 0;
	
	    this.position = [this.game.width + this.sprite.dimensions[0] / 2, 320];
	    this.scale = [-1, 1];
	  }
	
	  react(input) {
	    if (input === _keyframes.Special.Monster) {
	      this.hsp = -speed;
	    }
	  }
	
	  update() {
	    if (this.hsp || this.vsp) {
	      this.position[0] += this.hsp;
	      this.position[1] += this.vsp;
	      if (!Math.floor(--this.animationTimer)) {
	        this.subimage = (this.subimage + 1) % 4;
	        this.animationTimer += imageSpeed;
	      }
	    }
	  }
	}
	exports.default = Monster;

/***/ },
/* 46 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.rails = exports.train = undefined;
	
	var _background = __webpack_require__(14);
	
	var _background2 = _interopRequireDefault(_background);
	
	var _foreground = __webpack_require__(18);
	
	var _foreground2 = _interopRequireDefault(_foreground);
	
	var _train = __webpack_require__(90);
	
	var _train2 = _interopRequireDefault(_train);
	
	var _rails = __webpack_require__(89);
	
	var _rails2 = _interopRequireDefault(_rails);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const train = exports.train = new _background2.default(_train2.default);
	const rails = exports.rails = new _foreground2.default(_rails2.default);
	
	exports.default = Promise.all([train, rails].map(({ loaded }) => loaded));

/***/ },
/* 47 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(11);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(6);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const floorHeight = 630;
	
	class Coal extends _actor2.default {
	  constructor(position) {
	    super(_sprites.coal);
	    this.position = position;
	    this.hsp = Math.floor(Math.random() * 3 + 3);
	    this.vsp = -4;
	    this.deltaY = Math.floor(Math.random() * 10);
	  }
	
	  update() {
	    this.position[0] += this.hsp;
	    this.position[1] += this.vsp;
	    this.vsp += 0.5;
	    if (this.position[1] >= floorHeight - this.deltaY) {
	      this.vsp = -this.vsp / 2;
	      if (this.hsp) {
	        this.hsp -= Math.min(this.hsp, 1);
	      } else {
	        this.vsp = 0;
	      }
	    }
	  }
	}
	exports.default = Coal;

/***/ },
/* 48 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(11);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(6);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	class Grass extends _actor2.default {
	  constructor() {
	    super(_sprites.grass);
	    this.position = [_sprites.grass.dimensions[0], 0, -1000001];
	  }
	
	  update() {
	    this.position[0] -= 5;
	    if (this.position[0] <= 0) {
	      this.position[0] += _sprites.grass.dimensions[0];
	    }
	  }
	
	  draw(context) {
	    _sprites.grass.draw(context, 0, this.position[0], this.position[1], ..._sprites.grass.dimensions);
	    _sprites.grass.draw(context, 0, this.position[0] - _sprites.grass.dimensions[0], this.position[1], ..._sprites.grass.dimensions);
	    _sprites.grass.draw(context, 0, this.position[0] + _sprites.grass.dimensions[0], this.position[1], ..._sprites.grass.dimensions);
	  }
	}
	exports.default = Grass;

/***/ },
/* 49 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _enumerate = __webpack_require__(8);
	
	var _enumerate2 = _interopRequireDefault(_enumerate);
	
	var _actor = __webpack_require__(11);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _dave = __webpack_require__(27);
	
	var _dave2 = _interopRequireDefault(_dave);
	
	var _sprites = __webpack_require__(6);
	
	var _keyframes = __webpack_require__(20);
	
	var _const = __webpack_require__(2);
	
	var _coal = __webpack_require__(47);
	
	var _coal2 = _interopRequireDefault(_coal);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const speed = 2;
	const imageSpeed = 60 / 8;
	const knockAmount = 10;
	
	const Subimage = (0, _enumerate2.default)('Walk1', 'Walk2', 'Attack');
	
	class Monster extends _actor2.default {
	  constructor(position) {
	    super(_sprites.monster);
	    this.position = position;
	
	    this.animationTimer = imageSpeed;
	    this.walkFrame = 0;
	    this.knock = 0;
	    this.attacking = 0;
	  }
	
	  react(input) {
	    switch (input) {
	      case _keyframes.Special.Attack:
	        this.attacking = 60;
	        break;
	      case _keyframes.Special.Die:
	        for (let i = 0; i < 5; ++i) {
	          this.game.actors.push(new _coal2.default([this.position[0], this.position[1] + 100]));
	        }
	        this.destroy();
	        break;
	      case _const.Input.Attack:
	        if (!this.knock) {
	          this.knock = 10;
	        }
	        break;
	    }
	  }
	
	  update() {
	    const dir = -Math.sign(Math.abs(this.game.util.direction.from(this).to(_dave2.default)) - Math.PI / 2);
	    if (this.knock) {
	      this.position[0] -= dir * this.knock;
	      --this.knock;
	    } else if (this.attacking) {
	      this.subimage = Subimage.Attack;
	      --this.attacking;
	    } else {
	      if (!Math.floor(--this.animationTimer)) {
	        this.walkFrame = 1 - this.walkFrame;
	        this.animationTimer += imageSpeed;
	      }
	      this.subimage = this.walkFrame;
	      if (dir) {
	        this.scale[0] = -dir;
	      }
	      this.position[0] += speed * dir;
	    }
	  }
	}
	exports.default = Monster;

/***/ },
/* 50 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(11);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(6);
	
	var _keyframes = __webpack_require__(20);
	
	var _monster = __webpack_require__(49);
	
	var _monster2 = _interopRequireDefault(_monster);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const transformTime = 30;
	
	class Waitor extends _actor2.default {
	  constructor(game) {
	    super(_sprites.waitor);
	    this.position = [1000, 470];
	    this.transforming = 0;
	  }
	
	  react(input) {
	    if (input === _keyframes.Special.Transform) {
	      this.transforming = transformTime;
	    }
	  }
	
	  update() {
	    if (this.transforming) {
	      --this.transforming;
	      if (this.transforming % 4 === 0) {
	        if (this.sprite === _sprites.monster) {
	          this.sprite = _sprites.waitor;
	        } else {
	          this.sprite = _sprites.monster;
	        }
	      }
	      if (this.transforming === 0) {
	        this.game.actors.push(new _monster2.default(this.position));
	        this.destroy();
	      }
	    }
	  }
	}
	exports.default = Waitor;

/***/ },
/* 51 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _ = __webpack_require__(29);
	
	var _2 = _interopRequireDefault(_);
	
	var _actor = __webpack_require__(9);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(6);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	class Wheel extends (0, _actor2.default)(_2.default) {
	  constructor(position) {
	    super(_sprites.wheel);
	    this.position = [...position, 500];
	    this.angle = 0;
	  }
	
	  update() {
	    this.angle += 360 / 120;
	  }
	
	  draw(context) {
	    _sprites.wheel.drawRotated(context, 0, ...this.position.slice(0, 2), this.angle);
	  }
	}
	exports.default = Wheel;

/***/ },
/* 52 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	__webpack_require__(62);
	
	__webpack_require__(29);
	
	__webpack_require__(44);
	
	__webpack_require__(55);

/***/ },
/* 53 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.board = undefined;
	
	var _background = __webpack_require__(14);
	
	var _background2 = _interopRequireDefault(_background);
	
	var _foreground = __webpack_require__(18);
	
	var _foreground2 = _interopRequireDefault(_foreground);
	
	var _board = __webpack_require__(94);
	
	var _board2 = _interopRequireDefault(_board);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const board = exports.board = new _background2.default(_board2.default);
	
	exports.default = Promise.all([board].map(({ loaded }) => loaded));

/***/ },
/* 54 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(30);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(17);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const enterTime = 45;
	const waitTime = 105;
	const exitTime = 150;
	
	class Card extends _actor2.default {
	  constructor() {
	    super(_sprites.souvenir);
	    this.position = [this.game.width, -this.sprite.dimensions[1], 10000];
	    this.travelTimer = null;
	  }
	
	  react(input) {
	    if (input === _sprites.spring || input === _sprites.souvenir) {
	      this.sprite = input;
	      this.position = [this.game.width, -this.sprite.dimensions[1], 10000];
	      this.travelTimer = 0;
	    }
	  }
	
	  update() {
	    if (this.travelTimer !== null) {
	      let previous, destination, amt;
	      ++this.travelTimer;
	      if (this.travelTimer < enterTime) {
	        previous = [this.game.width, -this.sprite.dimensions[1], 10000];
	        destination = [this.game.width / 2, this.game.height / 2];
	        amt = Math.pow(Math.sin(this.travelTimer * Math.PI / 2 / enterTime), 2);
	      } else if (this.travelTimer > waitTime && this.travelTimer < exitTime) {
	        previous = [this.game.width / 2, this.game.height / 2];
	        destination = [this.game.width / 2, this.game.height + this.sprite.dimensions[1]];
	        amt = Math.pow(Math.sin((this.travelTimer - waitTime) * Math.PI / 2 / (exitTime - waitTime)), 2);
	      } else {
	        if (this.travelTimer > exitTime) {
	          this.travelTimer = null;
	        }
	        return;
	      }
	      const [dx, dy] = [destination[0] - previous[0], destination[1] - previous[1]];
	      this.x = previous[0] + dx * amt;
	      this.y = previous[1] + dy * amt;
	    }
	  }
	}
	exports.default = Card;

/***/ },
/* 55 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _generate = __webpack_require__(5);
	
	var _generate2 = _interopRequireDefault(_generate);
	
	var _sprites = __webpack_require__(17);
	
	var _sprites2 = _interopRequireDefault(_sprites);
	
	var _backgrounds = __webpack_require__(53);
	
	var _backgrounds2 = _interopRequireDefault(_backgrounds);
	
	var _game = __webpack_require__(31);
	
	var _game2 = _interopRequireDefault(_game);
	
	var _player = __webpack_require__(57);
	
	var _player2 = _interopRequireDefault(_player);
	
	var _card = __webpack_require__(54);
	
	var _card2 = _interopRequireDefault(_card);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	(0, _generate2.default)(function* () {
	  yield _sprites2.default;
	  yield _backgrounds2.default;
	  _game2.default.on('start', () => {
	    _game2.default.add.actor(new _player2.default(_sprites.green, [79, 290]), new _player2.default(_sprites.blue, [79, 360]), new _player2.default(_sprites.purple, [79, 430]), new _card2.default()).add.background(_backgrounds.board);
	  }).on('end', () => {
	    _game2.default.reset().play();
	  }).on('resize', () => {
	    _game2.default.canvas.style.width = window.innerWidth + 'px';
	  }).play();
	});

/***/ },
/* 56 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _const = __webpack_require__(2);
	
	var _keyframes = __webpack_require__(16);
	
	var _keyframes2 = _interopRequireDefault(_keyframes);
	
	var _sprites = __webpack_require__(17);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	exports.default = new _keyframes2.default().frame(1, [_sprites.purple], []).frame(2, [_sprites.souvenir], []).frame(7, [_sprites.blue], []).frame(8, [_sprites.spring], []).end(11);

/***/ },
/* 57 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(30);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(17);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const travelTime = 90;
	
	class Player extends _actor2.default {
	  constructor(sprite, position) {
	    super(sprite);
	    this.position = position;
	    this.previous = this.position;
	    this.destination = null;
	    this.travelTimer = 0;
	  }
	
	  react(input) {
	    if (input === this.sprite) {
	      switch (this.sprite) {
	        case _sprites.purple:
	          this.destination = [166, 205];
	          break;
	        case _sprites.blue:
	          this.destination = [434, 210];
	          break;
	      }
	    }
	  }
	
	  update() {
	    this.z = this.y;
	    if (this.destination !== null) {
	      const amt = Math.pow(Math.sin(++this.travelTimer * Math.PI / 2 / travelTime), 2);
	      const [dx, dy] = [this.destination[0] - this.previous[0], this.destination[1] - this.previous[1]];
	      this.x = this.previous[0] + dx * amt;
	      this.y = this.previous[1] + dy * amt;
	      if (this.travelTimer >= travelTime) {
	        this.travelTimer = 0;
	        this.previous = this.position;
	        this.destination = null;
	      }
	    }
	  }
	
	  draw(context) {
	    context.drawImage(this.sprite.image, ...this.position.slice(0, 2), 50, 80);
	  }
	}
	exports.default = Player;

/***/ },
/* 58 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _player = __webpack_require__(12);
	
	var _player2 = _interopRequireDefault(_player);
	
	var _sprites = __webpack_require__(3);
	
	var _const = __webpack_require__(7);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [PLAYER] = _symbolic2.default;
	const imageSpeed = 6;
	
	class Bird extends _actor2.default {
	  constructor(position) {
	    super(_sprites.bird);
	    this.position = position;
	    this.origin = [this.sprite.dimensions[0] / 2, this.sprite.dimensions[1]];
	    this.color = [_const.Color.Red, _const.Color.Green, _const.Color.White][this.game.util.random.integer.from(0).to(3)];
	    this.hsp = 0;
	    this.vsp = 0;
	  }
	
	  get player() {
	    return this[PLAYER] || (this[PLAYER] = this.game.actors.find(a => a instanceof _player2.default));
	  }
	
	  update() {
	    if (this.sprite === _sprites.bird) {
	      if (this.game.util.random.integer.from(0).to(90) === 0) {
	        this.imageTimer = imageSpeed;
	      }
	      if (this.game.util.distance.from(this).to(this.player) < 32) {
	        this.vsp = -2;
	        this.hsp = 1;
	        this.sprite = _sprites.bird_flying;
	        this.imageTimer = imageSpeed;
	      }
	    } else {
	      this.position[0] += this.hsp;
	      this.position[1] += this.vsp;
	      if (this.position[1] < 0) {
	        this.destroy();
	      }
	    }
	    if (this.imageTimer) {
	      --this.imageTimer;
	      if (!this.imageTimer) {
	        ++this.subimage;
	        if (this.sprite === _sprites.bird && this.subimage === 0) {
	          this.subimage = 0;
	        } else {
	          this.imageTimer = imageSpeed;
	        }
	      }
	    }
	    if (this.game.global.colors.has(this.color)) {
	      this.blend = this.color;
	    }
	  }
	}
	exports.default = Bird;

/***/ },
/* 59 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(3);
	
	var _const = __webpack_require__(7);
	
	var _player = __webpack_require__(12);
	
	var _player2 = _interopRequireDefault(_player);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [PLAYER] = _symbolic2.default;
	
	const imageSpeed = 3;
	const moveInterval = 4;
	class Butterfly extends _actor2.default {
	  constructor(position) {
	    super(_sprites.butterfly);
	    this.position = position;
	    this.startPosition = [...this.position];
	    this.color = [_const.Color.Red, _const.Color.Green, _const.Color.White][this.game.util.random.integer.between(0).and(3)];
	    this.imageTimer = imageSpeed;
	    this.moveTimer = moveInterval;
	  }
	
	  get player() {
	    return this[PLAYER] || (this[PLAYER] = this.game.actors.find(a => a instanceof _player2.default));
	  }
	
	  update() {
	    if (! --this.imageTimer) {
	      this.imageTimer = imageSpeed;
	      ++this.subimage;
	    }
	    if (! --this.moveTimer) {
	      this.moveTimer = moveInterval;
	      let [xmin, xmax, ymin, ymax] = [-2, 2, -2, 2];
	      if (this.game.util.distance.from(this).to(this.player) < 48) {
	        const [dx, dy] = [this.x - this.player.position[0], this.y - this.player.position[1]];
	        if (Math.abs(dx) < Math.abs(dy)) {
	          if (dx > 0) {
	            xmin += 3;
	            xmax += 3;
	          } else {
	            xmin -= 3;
	            xmax -= 3;
	          }
	        } else {
	          if (dy > 0) {
	            ymin += 3;
	            ymax += 3;
	          } else {
	            ymin -= 3;
	            ymax -= 3;
	          }
	        }
	      } else if (this.game.util.distance.from(this).to(this.startPosition) >= 32) {
	        const [dx, dy] = [this.x - this.startPosition[0], this.y - this.startPosition[1]];
	        if (Math.abs(dx) > 24) {
	          if (dx > 0) {
	            xmax -= 3;
	          } else {
	            xmin += 3;
	          }
	        }
	        if (Math.abs(dy) > 24) {
	          if (dy > 0) {
	            ymax -= 3;
	          } else {
	            ymin += 3;
	          }
	        }
	      }
	      this.x += this.game.util.random.integer.between(xmin).and(xmax);
	      this.y += this.game.util.random.integer.between(ymin).and(ymax);
	    }
	    if (this.game.global.colors.has(this.color)) {
	      this.blend = this.color;
	    }
	  }
	}
	exports.default = Butterfly;

/***/ },
/* 60 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(3);
	
	var _player = __webpack_require__(12);
	
	var _player2 = _interopRequireDefault(_player);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [PLAYER] = _symbolic2.default;
	
	class Door extends _actor2.default {
	  constructor(position, color) {
	    super(_sprites.door);
	    this.position = position;
	    this.color = color;
	    this.origin[1] = this.sprite.dimensions[1];
	  }
	
	  get player() {
	    return this[PLAYER] || (this[PLAYER] = this.game.actors.find(a => a instanceof _player2.default));
	  }
	
	  update() {
	    this.alpha = this.game.util.collision.between(this).and(this.player) ? this.alpha + 0.1 : this.alpha - 0.1;
	    this.alpha = Math.max(0.5, Math.min(this.alpha, 1));
	    if (this.game.global.colors.has(this.color)) {
	      this.blend = this.color;
	    }
	  }
	
	  draw(context) {
	    if (this.blend === this.color) {
	      super.draw(context);
	    }
	  }
	}
	exports.default = Door;

/***/ },
/* 61 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(3);
	
	var _const = __webpack_require__(7);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	class Flower extends _actor2.default {
	  constructor(position) {
	    super(_sprites.flower_w);
	    this.position = position;
	    this.position[2] = -1;
	    this.origin[1] = this.sprite.dimensions[1];
	  }
	
	  update() {
	    if (this.game.global.colors.has(_const.Color.Green)) {
	      this.sprite = _sprites.flower_g;
	    } else if (this.game.global.colors.has(_const.Color.Red)) {
	      this.sprite = _sprites.flower_r;
	    }
	  }
	}
	exports.default = Flower;

/***/ },
/* 62 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _generate = __webpack_require__(5);
	
	var _generate2 = _interopRequireDefault(_generate);
	
	var _sprites = __webpack_require__(3);
	
	var _sprites2 = _interopRequireDefault(_sprites);
	
	var _const = __webpack_require__(7);
	
	var _game = __webpack_require__(32);
	
	var _game2 = _interopRequireDefault(_game);
	
	var _player = __webpack_require__(12);
	
	var _player2 = _interopRequireDefault(_player);
	
	var _wall = __webpack_require__(66);
	
	var _wall2 = _interopRequireDefault(_wall);
	
	var _spark = __webpack_require__(34);
	
	var _spark2 = _interopRequireDefault(_spark);
	
	var _bird = __webpack_require__(58);
	
	var _bird2 = _interopRequireDefault(_bird);
	
	var _butterfly = __webpack_require__(59);
	
	var _butterfly2 = _interopRequireDefault(_butterfly);
	
	var _door = __webpack_require__(60);
	
	var _door2 = _interopRequireDefault(_door);
	
	var _flower = __webpack_require__(61);
	
	var _flower2 = _interopRequireDefault(_flower);
	
	var _tree = __webpack_require__(65);
	
	var _tree2 = _interopRequireDefault(_tree);
	
	var _switch = __webpack_require__(64);
	
	var _switch2 = _interopRequireDefault(_switch);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	(0, _generate2.default)(function* () {
	  yield _sprites2.default;
	  _game2.default.on('start', () => {
	    _game2.default.canvas.style.backgroundColor = '#ffffff';
	    let wallPos = 0;
	    _game2.default.add.actor(new _player2.default(), new _wall2.default([wallPos, 400], [256, _game2.default.height - 400]), new _wall2.default([wallPos += 256, 340], [128, _game2.default.height - 340]), new _wall2.default([wallPos += 128, 400], [448, _game2.default.height - 400]), new _wall2.default([wallPos += 448, 416], [64, _game2.default.height - 416], false), new _switch2.default([wallPos, 400], 200), new _wall2.default([wallPos += 64, 200], [128, _game2.default.height - 200]), new _wall2.default([wallPos += 128, 400], [Math.ceil((_game2.default.width - wallPos) / 16) * 16, _game2.default.height - 400]), new _spark2.default([300, 320], _const.Color.Red), new _spark2.default([680, 380], _const.Color.Green), new _spark2.default([1200, 380], _const.Color.White), new _door2.default([1400, 400], _const.Color.Green), new _tree2.default([276, 340]), new _tree2.default([660, 400]), new _tree2.default([1500, 400]), new _flower2.default([296, 340]), new _flower2.default([988, 200]), new _flower2.default([_game2.default.util.random.integer.from(20).to(230), 400]), new _flower2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _flower2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _flower2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _flower2.default([_game2.default.util.random.integer.from(1100).to(1600), 400]), new _flower2.default([_game2.default.util.random.integer.from(1100).to(1600), 400]), new _flower2.default([_game2.default.util.random.integer.from(1100).to(1600), 400]), new _bird2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _bird2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _bird2.default([_game2.default.util.random.integer.from(1100).to(1600), 400]), new _butterfly2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _butterfly2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _butterfly2.default([_game2.default.util.random.integer.from(400).to(700), 400]), new _butterfly2.default([_game2.default.util.random.integer.from(1100).to(1600), 400]), new _butterfly2.default([_game2.default.util.random.integer.from(1100).to(1600), 400]), new _butterfly2.default([_game2.default.util.random.integer.from(1100).to(1600), 400])).global.colors = new Set([_const.Color.White]);
	  }).on('end', () => {
	    _game2.default.reset().play();
	  }).on('resize', () => {
	    _game2.default.canvas.style.width = window.innerWidth + 'px';
	  }).play();
	});

/***/ },
/* 63 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(3);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const imageSpeed = 4;
	class Puff extends _actor2.default {
	  constructor(position, direction) {
	    super(_sprites.puff);
	    if (direction === 1) {
	      this.subimage = 4;
	    }
	    this.position = position;
	    this.imageTimer = imageSpeed;
	    this.origin[1] = this.sprite.dimensions[1];
	  }
	
	  update() {
	    if (! --this.imageTimer) {
	      this.imageTimer = imageSpeed;
	      ++this.subimage;
	      if (this.subimage === 4 || this.subimage === 0) {
	        this.destroy();
	      }
	    }
	  }
	}
	exports.default = Puff;

/***/ },
/* 64 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _solid = __webpack_require__(21);
	
	var _solid2 = _interopRequireDefault(_solid);
	
	var _sprites = __webpack_require__(3);
	
	var _const = __webpack_require__(7);
	
	var _keyframes = __webpack_require__(33);
	
	var _player = __webpack_require__(12);
	
	var _player2 = _interopRequireDefault(_player);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [PLAYER] = _symbolic2.default;
	
	const maxSparks = 16;
	class Switch extends _solid2.default {
	  constructor(position, vdist) {
	    super(_sprites.switch_platform);
	    this.position = position;
	    this.origin[0] = 0;
	    this.ystart = this.y;
	    this.vdist = vdist;
	    this.sparks = [];
	  }
	
	  get player() {
	    return this[PLAYER] || (this[PLAYER] = this.game.actors.find(a => a instanceof _player2.default));
	  }
	
	  react(input) {
	    if (input === _keyframes.Special.Elevator && this.game.util.collision.between(this).and(this.player)) {
	      this.active = true;
	    }
	  }
	
	  update() {
	    if (this.active) {
	      if (this.y > this.ystart - this.vdist) {
	        this.y -= 2;
	        this.player.y -= 2;
	        this.sparks.unshift([this.game.util.random.integer.between(this.x).and(this.x + this.dimensions[0]), this.y + 16]);
	      } else {
	        this.sparks = [];
	      }
	      this.subimage = 1;
	      this.active = false;
	    } else {
	      this.subimage = 0;
	      if (this.y < this.ystart) {
	        this.y += 2;
	        this.sparks.unshift([this.game.util.random.integer.between(this.x).and(this.x + this.dimensions[0]), this.y]);
	      } else {
	        this.sparks = [];
	      }
	    }
	    if (this.sparks.length > maxSparks) {
	      this.sparks = this.sparks.slice(0, maxSparks);
	    }
	  }
	
	  draw(context) {
	    super.draw(context);
	    if (this.game.global.colors.has(_const.Color.Green) && this.subimage === 1) {
	      context.fillStyle = '#008000';
	    } else if (this.game.global.colors.has(_const.Color.Red)) {
	      context.fillStyle = '#FF0000';
	    } else {
	      context.fillStyle = '#FFFFFF';
	    }
	    context.beginPath();
	    context.arc(this.x + 32, this.y, 2, 0, Math.PI * 2);
	    context.fill();
	    context.fillStyle = '#FFFFFF';
	    for (let spark of this.sparks) {
	      context.fillRect(...spark, 1, 1);
	    }
	  }
	}
	exports.default = Switch;

/***/ },
/* 65 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _actor = __webpack_require__(4);
	
	var _actor2 = _interopRequireDefault(_actor);
	
	var _sprites = __webpack_require__(3);
	
	var _const = __webpack_require__(7);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	class Tree extends _actor2.default {
	  constructor(position) {
	    super(_sprites.tree_w);
	    this.position = position;
	    this.position[2] = -1;
	    this.origin[1] = this.sprite.dimensions[1];
	  }
	
	  update() {
	    if (this.game.global.colors.has(_const.Color.Green)) {
	      this.sprite = _sprites.tree_g;
	    } else if (this.game.global.colors.has(_const.Color.Red)) {
	      this.sprite = _sprites.tree_r;
	    }
	  }
	}
	exports.default = Tree;

/***/ },
/* 66 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	var _solid = __webpack_require__(21);
	
	var _solid2 = _interopRequireDefault(_solid);
	
	var _const = __webpack_require__(7);
	
	var _sprites = __webpack_require__(3);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [DIMENSIONS] = _symbolic2.default;
	
	class Wall extends _solid2.default {
	  constructor(position, dimensions, hasGrass = true) {
	    super();
	    this.position = [...position, -10];
	    this[DIMENSIONS] = dimensions;
	    this.grass = [];
	    if (hasGrass) {
	      while (this.grass.length < this.dimensions[0] / _sprites.grass.dimensions[0]) {
	        this.grass.push(this.game.util.random.integer.between(0).and(5));
	      }
	    }
	  }
	
	  get dimensions() {
	    return this[DIMENSIONS];
	  }
	
	  draw(context) {
	    context.fillStyle = '#ffffff';
	    context.fillRect(...this.position.slice(0, 2), ...this.dimensions);
	    if (this.game.global.colors.has(_const.Color.Green)) {
	      for (let i = 0; i < this.grass.length; ++i) {
	        _sprites.grass.draw(context, this.grass[i], this.position[0] + i * _sprites.grass.dimensions[0], this.position[1] - _sprites.grass.dimensions[1]);
	      }
	    }
	  }
	}
	exports.default = Wall;

/***/ },
/* 67 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.default = border;
	
	var _svg = __webpack_require__(35);
	
	var SVG = _interopRequireWildcard(_svg);
	
	var _onResize = __webpack_require__(13);
	
	var _onResize2 = _interopRequireDefault(_onResize);
	
	var _media = __webpack_require__(22);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }
	
	const svg = document.querySelector('#header-border');
	const pathstr = 'M 102.342 18.469 C 103.487 9.722 87.048 1.975 76.015 4.733 C 62.536 8.103 55.473 17.155 50.833 29.915 C 34.031 76.121 98.523 184.441 7.337 184.441 L 7.337 185.586';
	const animationTime = 1500;
	const paths = [];
	
	while (paths.length < 4) {
	  const path = SVG.createElement('path');
	  SVG.setAttribute(path, 'd', pathstr);
	  const len = path.getTotalLength();
	  SVG.setAttribute(path, 'stroke-dasharray', `${ len } 0`);
	  SVG.setAttribute(path, 'transform-origin', '100% 100%');
	  svg.appendChild(path);
	  paths.push(path);
	}
	
	function transform() {
	  let x = 50;
	  if (_media.phone.matches) {
	    x = 0;
	  }
	  // mobile devices height changes when the page scrolls and the navbar expands
	  // but that makes this look stupid
	  const y = (window.IS_MOBILE ? window.orientation === 0 ? screen.height : screen.width : window.innerHeight) / 2;
	  const { height: h } = paths[0].getBoundingClientRect();
	
	  SVG.setAttribute(paths[0], 'transform', `translate(${ x } ${ y - h })`);
	  SVG.setAttribute(paths[1], 'transform', `translate(${ x } ${ y + h }) scale(1 -1)`);
	  SVG.setAttribute(paths[2], 'transform', `translate(${ window.innerWidth - x } ${ y - h }) scale(-1, 1)`);
	  SVG.setAttribute(paths[3], 'transform', `translate(${ window.innerWidth - x } ${ y + h }) scale(-1, -1)`);
	}
	(0, _onResize2.default)(transform);
	
	function border() {
	  transform();
	  paths.forEach(path => SVG.animate(path, animationTime));
	}

/***/ },
/* 68 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _generate = __webpack_require__(5);
	
	var _generate2 = _interopRequireDefault(_generate);
	
	var _wait = __webpack_require__(40);
	
	var _wait2 = _interopRequireDefault(_wait);
	
	var _prompt = __webpack_require__(69);
	
	var _prompt2 = _interopRequireDefault(_prompt);
	
	var _border = __webpack_require__(67);
	
	var _border2 = _interopRequireDefault(_border);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	let prompt = new _prompt2.default(document.querySelector('.prompt'));
	
	const skip = () => end();
	let ended = false;
	function end() {
	  if (ended) {
	    return;
	  }
	  ended = true;
	  document.body.classList.remove('loading');
	  document.body.classList.add('loaded');
	  window.setTimeout(() => {
	    const loading = document.querySelector('.loading');
	    loading.parentNode.removeChild(loading);
	  }, 0);
	  window.removeEventListener('click', skip);
	  window.removeEventListener('wheel', skip);
	  window.removeEventListener('keydown', skip);
	  (0, _border2.default)();
	}
	
	(0, _generate2.default)(function* () {
	  yield (0, _wait2.default)(500);
	  yield prompt.setCommand('cd public_html');
	  yield (0, _wait2.default)(100);
	  prompt = prompt.next();
	  yield (0, _wait2.default)(500);
	  yield prompt.setCommand('npm start');
	  yield (0, _wait2.default)(100);
	  prompt = prompt.next();
	  prompt.prompt = '';
	  prompt.command = 'Starting server';
	  for (let i = 0; i < 3; ++i) {
	    yield (0, _wait2.default)(250);
	    prompt.command += '.';
	  }
	  yield (0, _wait2.default)(250);
	  end();
	});
	
	window.addEventListener('click', skip);
	window.addEventListener('wheel', skip);
	window.addEventListener('keydown', skip);

/***/ },
/* 69 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _symbolic = __webpack_require__(1);
	
	var _symbolic2 = _interopRequireDefault(_symbolic);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const [ELEMENT, PROMPT, COMMAND, UPDATER] = _symbolic2.default;
	
	class Prompt {
	  constructor(element) {
	    this[ELEMENT] = element;
	    this[PROMPT] = '&gt;';
	    this[COMMAND] = '';
	    this[UPDATER] = window.requestAnimationFrame(this.update.bind(this));
	  }
	
	  set prompt(prompt) {
	    this[PROMPT] = prompt;
	  }
	  get prompt() {
	    return this[PROMPT];
	  }
	
	  setCommand(command) {
	    return new Promise((resolve, reject) => {
	      let i = 0;
	      this[COMMAND] = '';
	      const addLetter = () => {
	        if (i < command.length) {
	          this[COMMAND] += command[i++];
	          window.setTimeout(addLetter, 50);
	        } else {
	          resolve();
	        }
	      };
	      window.setTimeout(addLetter, 50);
	    });
	  }
	  set command(command) {
	    this[COMMAND] = command;
	    this[ELEMENT].innerHTML = this.prompt + ' ' + this.command;
	    window.cancelAnimationFrame(this[UPDATER]);
	  }
	  get command() {
	    return this[COMMAND];
	  }
	
	  update(time) {
	    this[ELEMENT].innerHTML = this.prompt + ' ' + this.command + (Math.floor(time / 500) % 2 ? '_' : '');
	    this[UPDATER] = window.requestAnimationFrame(this.update.bind(this));
	  }
	
	  next() {
	    this[ELEMENT].innerHTML = this.prompt + ' ' + this.command;
	    window.cancelAnimationFrame(this[UPDATER]);
	    const next = document.createElement('div');
	    next.classList.add('prompt');
	    this[ELEMENT].parentNode.appendChild(next);
	    return new Prompt(next);
	  }
	}
	exports.default = Prompt;

/***/ },
/* 70 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _svg = __webpack_require__(35);
	
	var SVG = _interopRequireWildcard(_svg);
	
	var _onResize = __webpack_require__(13);
	
	var _onResize2 = _interopRequireDefault(_onResize);
	
	var _media = __webpack_require__(22);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }
	
	const svgs = document.querySelectorAll('.burst');
	const pathstr = 'M 242.045 26.845 C 235.174 15.265 252.673 9.194 256.562 20.331 C 257.28 22.387 258.362 27.943 255.469 32.642 C 250.15 41.281 241.689 42.842 230.909 40.209 C 214.19 36.125 207.736 32.195 196.389 26.652 C 184.111 20.654 176.454 18.94 169.089 18.449 C 163.47 18.075 160.195 18.053 156.118 18.206 C 152.733 18.333 141.704 19.696 137.327 20.434 C 132.629 21.226 129.324 21.96 131.407 21.538 M 156.898 33.347 C 154.215 32.231 149.785 33.435 148.194 36.36 C 146.855 38.822 147.471 42.212 149.355 43.688 C 151.649 45.485 154.179 45.931 158.491 44.482 C 169.188 40.887 169.512 23.332 154.909 20.404 C 136.023 16.617 113.006 28.284 94.665 30.179 C 87.387 30.931 65.969 31.376 51.021 28.409 M 92.259 12.639 C 91.942 13.633 90.511 14.806 88.619 14.917 C 83.811 15.198 82.791 10.379 86.203 7.199 C 90.86 2.859 100.116 8.285 96.713 16.822 C 92.769 26.714 76.995 29.629 67.758 29.629 C 46.664 29.629 26.524 22.39 5.393 21.833';
	const animationTime = 1200;
	
	Array.prototype.forEach.call(svgs, svg => {
	  const paths = [];
	  while (paths.length < 2) {
	    const path = SVG.createElement('path');
	    SVG.setAttribute(path, 'd', pathstr);
	    const len = path.getTotalLength();
	    SVG.setAttribute(path, 'stroke-dasharray', `${ len }`);
	    SVG.setAttribute(path, 'stroke-dashoffset', `${ len }`);
	    SVG.setAttribute(path, 'transform-origin', '0 50%');
	    svg.appendChild(path);
	    paths.push(path);
	  }
	  function transform() {
	    let scale = 1;
	    if (_media.phone.matches) {
	      scale = 0.4;
	    } else if (_media.tablet.matches) {
	      scale = 0.55;
	    }
	    const y = svg.getBoundingClientRect().height / 2 - paths[0].getBoundingClientRect().height / 2 * scale;
	    SVG.setAttribute(paths[0], 'transform', `translate(${ window.innerWidth / 2 - 200 * scale } ${ y }) scale(-${ scale } ${ scale })`);
	    SVG.setAttribute(paths[1], 'transform', `translate(${ window.innerWidth / 2 + 200 * scale } ${ y }) scale(${ scale } ${ scale })`);
	  }
	  (0, _onResize2.default)(transform);
	  window.setTimeout(transform, 0);
	
	  window.addEventListener('scroll', function scrollListener() {
	    if (document.body.scrollTop > svg.getBoundingClientRect().bottom + 100 - window.innerHeight) {
	      paths.forEach(path => SVG.animate(path, animationTime));
	      window.removeEventListener('scroll', scrollListener);
	    }
	  });
	});

/***/ },
/* 71 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	__webpack_require__(70);
	
	__webpack_require__(72);

/***/ },
/* 72 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _generate = __webpack_require__(5);
	
	var _generate2 = _interopRequireDefault(_generate);
	
	var _onResize = __webpack_require__(13);
	
	var _onResize2 = _interopRequireDefault(_onResize);
	
	var _media = __webpack_require__(22);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	const images = [];
	Array.prototype.forEach.call(document.querySelectorAll('.logo'), logo => {
	  (0, _generate2.default)(function* () {
	    const name = [...logo.classList].filter(c => c !== 'logo')[0];
	    const src = `images/logos/${ name }.svg`;
	    const img = new Image();
	    img.src = src;
	    img.height = 80;
	    logo.appendChild(img);
	    images.push(img);
	  });
	});
	
	(0, _onResize2.default)(() => {
	  let h = 80;
	  if (_media.phone.matches) {
	    h = 40;
	  } else if (_media.tablet.matches) {
	    h = 60;
	  }
	  images.forEach(img => img.height = h);
	});

/***/ },
/* 73 */
/***/ function(module, exports, __webpack_require__) {

	exports = module.exports = __webpack_require__(36)();
	// imports
	
	
	// module
	exports.push([module.id, "a,abbr,acronym,address,applet,article,aside,audio,b,big,blockquote,body,canvas,caption,center,cite,code,dd,del,details,dfn,div,dl,dt,em,embed,fieldset,figcaption,figure,footer,form,h1,h2,h3,h4,h5,h6,header,hgroup,html,i,iframe,img,ins,kbd,label,legend,li,mark,menu,nav,object,ol,output,p,pre,q,ruby,s,samp,section,small,span,strike,strong,sub,summary,sup,table,tbody,td,tfoot,th,thead,time,tr,tt,u,ul,var,video{margin:0;padding:0;border:0;font-size:100%;font:inherit;vertical-align:baseline}article,aside,details,figcaption,figure,footer,header,hgroup,menu,nav,section{display:block}body{line-height:1}ol,ul{list-style:none}blockquote,q{quotes:none}blockquote:after,blockquote:before,q:after,q:before{content:'';content:none}table{border-collapse:collapse;border-spacing:0}", ""]);
	
	// exports


/***/ },
/* 74 */
/***/ function(module, exports, __webpack_require__) {

	exports = module.exports = __webpack_require__(36)();
	// imports
	exports.push([module.id, "@import url(https://fonts.googleapis.com/css?family=Bungee+Hairline|Source+Code+Pro:200|Raleway:200,300);", ""]);
	
	// module
	exports.push([module.id, ".container{position:relative}body{display:inline;display:initial;overflow-x:hidden;font-family:Raleway,sans-serif;font-weight:200}@media only screen and (min-width:640px) and (max-width:960px){body{font-size:14pt}}@media only screen and (min-width:961px){body{font-size:16pt}}section:not(.title){background-image:url(" + __webpack_require__(23) + ");padding:60px 0}@media only screen and (min-width:961px){section:not(.title)#games{padding-top:0}}canvas,svg{pointer-events:none}svg{position:absolute;top:0;left:0;width:100%;height:100%;fill:none;stroke:#000;stroke-width:1}a{text-decoration:none}a:link,a:visited{color:#2e62b0}a:hover{text-decoration:underline;color:#528fe0}body.loading .loaded{height:0;overflow:hidden;opacity:0}body.loading .loaded hgroup{line-height:0}body.loaded .loading{display:none}body.loaded .loaded{background-image:url(" + __webpack_require__(23) + ");opacity:1;transition:opacity 1.5s}@media only screen and (min-width:961px){body.loaded .loaded{background-image:url(" + __webpack_require__(37) + ");background-attachment:fixed;background-size:cover}}.commands{padding:30px}.prompt{font-family:Source Code Pro,monospace;font-size:14pt}footer,header{box-sizing:border-box;width:100vw;height:100vh;display:flex;justify-content:center;flex-direction:column}footer.page-header,header.page-header{background:url(" + __webpack_require__(37) + ");background-size:cover;background-position:top}@media only screen and (min-width:961px){footer.page-header,header.page-header{background-attachment:fixed}}footer{background:url(" + __webpack_require__(23) + ")}hgroup{box-sizing:border-box;position:relative;display:flex;justify-content:center;flex-direction:column;width:100%;height:500px;transition:line-height 1.5s;padding:0 125px;background:hsla(0,0%,100%,.8)}@media only screen and (max-width:640px){hgroup{padding:0 75px}}h1,h2,h3,h4,h6{font-family:Bungee Hairline,sans-serif;text-align:center}@media only screen and (max-width:640px){h1{font-size:30pt}h2{font-size:20pt}.page-header h2{font-size:15pt}h3{font-size:20pt}h4{font-size:15pt}}@media only screen and (min-width:640px) and (max-width:960px){h1{font-size:40pt}h2,h3{font-size:25pt}h4{font-size:20pt}}@media only screen and (min-width:961px){h1{font-size:65pt}h2,h3{font-size:35pt}h4{font-size:25pt}}h6{margin-top:auto}hr{position:relative;border:none;background-color:rgba(0,0,0,.5);width:35%;height:1px;margin:10pt auto}hr:after{content:'///';position:absolute;left:calc(50% - 1ex);top:-10pt;color:#000;font-family:Bungee Hairline,sans-serif;font-size:20pt}@media only screen and (min-width:961px){section.title{padding-top:60px}section.title:not(.image){padding-bottom:60px;background:hsla(0,0%,100%,.7)}}section#about .container{display:flex;max-width:1200px;flex-basis:1200px;margin:0 auto;flex-grow:0;flex-shrink:1;flex-wrap:wrap}section#about .container div{margin:1pc}@media only screen and (max-width:640px){section#about .container div{margin:5px}}section#about .container #bio{flex-basis:400px;flex-grow:1}section#about .container #experience{flex-basis:1200px;flex-shrink:1}.avatar{width:200px;height:200px;margin:0 auto;border-radius:100%;background-image:url(" + __webpack_require__(75) + ");background-size:contain}@media only screen and (max-width:640px){.avatar{width:100px;height:100px}}.card{box-sizing:border-box;height:calc(300px + 2pc);flex-basis:400px;padding:1pc;border-radius:6px;border:1px solid rgba(0,0,0,.5);display:flex;justify-content:space-between;flex-grow:1;box-shadow:3px 3px 5px rgba(0,0,0,.3)}.card img{flex-grow:0}.card>img{width:300px;height:300px}@media only screen and (max-width:640px){.card>img{width:200px;height:200px}}dl{display:flex;flex-direction:column;vertical-align:top;flex-grow:1;margin-left:1pc}dt{font-weight:300;margin-top:1pc}dd{font-weight:200;margin-top:calc(.5pc - 4px);margin-bottom:-4px;width:100%;overflow-x:hidden;overflow-y:visible;padding:4px 0;word-wrap:nowrap;text-overflow:ellipsis}dd.bottom{margin-bottom:0;margin-top:auto;padding:0;overflow:hidden}#skills{display:flex;flex-wrap:wrap;justify-content:center}#skills .container{width:1200px;display:flex;flex-direction:column}#skills .container div{display:flex;justify-content:center;flex-wrap:wrap}#skills .logo{margin:40px}@media only screen and (max-width:640px){#skills .logo{margin:5px}}.info{box-sizing:border-box;width:100%;max-width:1200px;margin:0 auto;padding:1pc}@media only screen and (max-width:640px){.info{padding:5px}}.info .close{display:none}article{display:flex;flex-direction:column;line-height:normal}article footer,article header{width:100%;height:auto;line-height:1}article footer hr,article header hr{width:70%}@media only screen and (max-width:640px){article footer hr,article header hr{width:50%}article footer hr:after,article header hr:after{font-size:14pt;top:-7pt}}article footer p,article header p{padding:0 10px}p{margin-bottom:10px}#sites{position:relative;display:flex;flex-wrap:wrap;line-height:0}#sites div{position:relative;box-sizing:border-box;width:100vw}@media only screen and (min-width:961px){#sites div{width:50%}}#sites div img{width:100%}#sites div article{position:absolute;top:0;left:0;right:0;bottom:0;display:flex;justify-content:center}#sites div article>a,#sites div article>header{opacity:0;display:flex;height:50%;background-color:hsla(0,0%,100%,.95);transition:opacity .5s}#sites div article:hover a,#sites div article:hover header{opacity:1}", ""]);
	
	// exports


/***/ },
/* 75 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/e70f0a2561f934e92237c123f7fca499.png";

/***/ },
/* 76 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/f0d742df2fd14ca33d1b7fb26ecc3c34.png";

/***/ },
/* 77 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/65957b12d94a60998876926150cb5fe3.png";

/***/ },
/* 78 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/5e498bb1f48e82a462034cd4914c256e.png";

/***/ },
/* 79 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/d20d7985280bcf4a37d2b2365a0bfb2f.png";

/***/ },
/* 80 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/fb153981988f3329beaec73c3bf51682.png";

/***/ },
/* 81 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/80f1b52f3e66262418e6a10d3e7276a9.png";

/***/ },
/* 82 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/672c55c45210ffb10b6183297173020b.png";

/***/ },
/* 83 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/7814cb68564652c0e9ca3c3f0021e7e9.png";

/***/ },
/* 84 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/de7abfd1d69a4e534db12016a932f203.png";

/***/ },
/* 85 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/55f76bd832d1a930f708ee301875c2d8.png";

/***/ },
/* 86 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/d40afc8f031f80c7859dff750d8e722e.png";

/***/ },
/* 87 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/d41339c86b501dfbce72ab1d2ee1938e.png";

/***/ },
/* 88 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/3d9245dc669061e63fcbe8573814bf9d.png";

/***/ },
/* 89 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/037ea9be295d8ddb9325d58cc6ef578a.png";

/***/ },
/* 90 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/880b313a0f82023b0bb3ea43cfa7402f.png";

/***/ },
/* 91 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/ddf28ace11c15ff15884f6ef9e3b5abe.png";

/***/ },
/* 92 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/eb3998a8a6ec8f7ce0139b3f6dc55e16.png";

/***/ },
/* 93 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/b848db66bf367ec4bdd32ab059bd2b02.svg";

/***/ },
/* 94 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/10cf17e911ec0e4526cc6f64f5a373d9.jpg";

/***/ },
/* 95 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/eb268d97713623b576cd9cdf7eb27bf9.svg";

/***/ },
/* 96 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/605507c961cf5fc51fe6bda42ed0483e.svg";

/***/ },
/* 97 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/c09d4086f406f3b20972faefc663d023.jpg";

/***/ },
/* 98 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/ef9beec1e7aa98271cdf6e071794f416.jpg";

/***/ },
/* 99 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/2b646b356de38cdcb75ba1695e940858.png";

/***/ },
/* 100 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/60a1df11cb235d187145765e929735e0.png";

/***/ },
/* 101 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/b64b8c012b47364bea2dafbe6c2d6c7b.png";

/***/ },
/* 102 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/53e55ac0753640617609a05925158908.png";

/***/ },
/* 103 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/a29febf46596526ab40efd3b9f796642.png";

/***/ },
/* 104 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/d943d9557c168c6cc1eef4246b7a6562.png";

/***/ },
/* 105 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/3910ab5a5480eb1d688582054f16b807.png";

/***/ },
/* 106 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/ad7d53c4c2479f362195cc10bf68371a.png";

/***/ },
/* 107 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/771720c76d822674e7dd2a5768fa1dd1.png";

/***/ },
/* 108 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/d37d376a684554681424a374241fae96.png";

/***/ },
/* 109 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/4af89cafefe84e0a8c66cf9224bb4216.png";

/***/ },
/* 110 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/9bed8611d445040af7a55b4154c45f00.png";

/***/ },
/* 111 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/49c566897f64efa1d6c93f85830811a8.png";

/***/ },
/* 112 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/56b5ae3f382fbf7bdc201639441efed0.png";

/***/ },
/* 113 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__.p + "images/ddc669f759dbf0dd71aa5fad08903735.png";

/***/ },
/* 114 */
/***/ function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_RESULT__;/* WEBPACK VAR INJECTION */(function(module, global) {/*!
	 * Platform.js v1.3.1 <http://mths.be/platform>
	 * Copyright 2014-2016 Benjamin Tan <https://d10.github.io/>
	 * Copyright 2011-2013 John-David Dalton <http://allyoucanleet.com/>
	 * Available under MIT license <http://mths.be/mit>
	 */
	;(function() {
	  'use strict';
	
	  /** Used to determine if values are of the language type `Object` */
	  var objectTypes = {
	    'function': true,
	    'object': true
	  };
	
	  /** Used as a reference to the global object */
	  var root = (objectTypes[typeof window] && window) || this;
	
	  /** Backup possible global object */
	  var oldRoot = root;
	
	  /** Detect free variable `exports` */
	  var freeExports = objectTypes[typeof exports] && exports;
	
	  /** Detect free variable `module` */
	  var freeModule = objectTypes[typeof module] && module && !module.nodeType && module;
	
	  /** Detect free variable `global` from Node.js or Browserified code and use it as `root` */
	  var freeGlobal = freeExports && freeModule && typeof global == 'object' && global;
	  if (freeGlobal && (freeGlobal.global === freeGlobal || freeGlobal.window === freeGlobal || freeGlobal.self === freeGlobal)) {
	    root = freeGlobal;
	  }
	
	  /**
	   * Used as the maximum length of an array-like object.
	   * See the [ES6 spec](http://people.mozilla.org/~jorendorff/es6-draft.html#sec-tolength)
	   * for more details.
	   */
	  var maxSafeInteger = Math.pow(2, 53) - 1;
	
	  /** Opera regexp */
	  var reOpera = /\bOpera/;
	
	  /** Possible global object */
	  var thisBinding = this;
	
	  /** Used for native method references */
	  var objectProto = Object.prototype;
	
	  /** Used to check for own properties of an object */
	  var hasOwnProperty = objectProto.hasOwnProperty;
	
	  /** Used to resolve the internal `[[Class]]` of values */
	  var toString = objectProto.toString;
	
	  /*--------------------------------------------------------------------------*/
	
	  /**
	   * Capitalizes a string value.
	   *
	   * @private
	   * @param {string} string The string to capitalize.
	   * @returns {string} The capitalized string.
	   */
	  function capitalize(string) {
	    string = String(string);
	    return string.charAt(0).toUpperCase() + string.slice(1);
	  }
	
	  /**
	   * A utility function to clean up the OS name.
	   *
	   * @private
	   * @param {string} os The OS name to clean up.
	   * @param {string} [pattern] A `RegExp` pattern matching the OS name.
	   * @param {string} [label] A label for the OS.
	   */
	  function cleanupOS(os, pattern, label) {
	    // platform tokens defined at
	    // http://msdn.microsoft.com/en-us/library/ms537503(VS.85).aspx
	    // http://web.archive.org/web/20081122053950/http://msdn.microsoft.com/en-us/library/ms537503(VS.85).aspx
	    var data = {
	      '6.4':  '10',
	      '6.3':  '8.1',
	      '6.2':  '8',
	      '6.1':  'Server 2008 R2 / 7',
	      '6.0':  'Server 2008 / Vista',
	      '5.2':  'Server 2003 / XP 64-bit',
	      '5.1':  'XP',
	      '5.01': '2000 SP1',
	      '5.0':  '2000',
	      '4.0':  'NT',
	      '4.90': 'ME'
	    };
	    // detect Windows version from platform tokens
	    if (pattern && label && /^Win/i.test(os) &&
	        (data = data[0/*Opera 9.25 fix*/, /[\d.]+$/.exec(os)])) {
	      os = 'Windows ' + data;
	    }
	    // correct character case and cleanup
	    os = String(os);
	
	    if (pattern && label) {
	      os = os.replace(RegExp(pattern, 'i'), label);
	    }
	
	    os = format(
	      os.replace(/ ce$/i, ' CE')
	        .replace(/\bhpw/i, 'web')
	        .replace(/\bMacintosh\b/, 'Mac OS')
	        .replace(/_PowerPC\b/i, ' OS')
	        .replace(/\b(OS X) [^ \d]+/i, '$1')
	        .replace(/\bMac (OS X)\b/, '$1')
	        .replace(/\/(\d)/, ' $1')
	        .replace(/_/g, '.')
	        .replace(/(?: BePC|[ .]*fc[ \d.]+)$/i, '')
	        .replace(/\bx86\.64\b/gi, 'x86_64')
	        .replace(/\b(Windows Phone) OS\b/, '$1')
	        .split(' on ')[0]
	    );
	
	    return os;
	  }
	
	  /**
	   * An iteration utility for arrays and objects.
	   *
	   * @private
	   * @param {Array|Object} object The object to iterate over.
	   * @param {Function} callback The function called per iteration.
	   */
	  function each(object, callback) {
	    var index = -1,
	        length = object ? object.length : 0;
	
	    if (typeof length == 'number' && length > -1 && length <= maxSafeInteger) {
	      while (++index < length) {
	        callback(object[index], index, object);
	      }
	    } else {
	      forOwn(object, callback);
	    }
	  }
	
	  /**
	   * Trim and conditionally capitalize string values.
	   *
	   * @private
	   * @param {string} string The string to format.
	   * @returns {string} The formatted string.
	   */
	  function format(string) {
	    string = trim(string);
	    return /^(?:webOS|i(?:OS|P))/.test(string)
	      ? string
	      : capitalize(string);
	  }
	
	  /**
	   * Iterates over an object's own properties, executing the `callback` for each.
	   *
	   * @private
	   * @param {Object} object The object to iterate over.
	   * @param {Function} callback The function executed per own property.
	   */
	  function forOwn(object, callback) {
	    for (var key in object) {
	      if (hasOwnProperty.call(object, key)) {
	        callback(object[key], key, object);
	      }
	    }
	  }
	
	  /**
	   * Gets the internal `[[Class]]` of a value.
	   *
	   * @private
	   * @param {*} value The value.
	   * @returns {string} The `[[Class]]`.
	   */
	  function getClassOf(value) {
	    return value == null
	      ? capitalize(value)
	      : toString.call(value).slice(8, -1);
	  }
	
	  /**
	   * Host objects can return type values that are different from their actual
	   * data type. The objects we are concerned with usually return non-primitive
	   * types of "object", "function", or "unknown".
	   *
	   * @private
	   * @param {*} object The owner of the property.
	   * @param {string} property The property to check.
	   * @returns {boolean} Returns `true` if the property value is a non-primitive, else `false`.
	   */
	  function isHostType(object, property) {
	    var type = object != null ? typeof object[property] : 'number';
	    return !/^(?:boolean|number|string|undefined)$/.test(type) &&
	      (type == 'object' ? !!object[property] : true);
	  }
	
	  /**
	   * Prepares a string for use in a `RegExp` by making hyphens and spaces optional.
	   *
	   * @private
	   * @param {string} string The string to qualify.
	   * @returns {string} The qualified string.
	   */
	  function qualify(string) {
	    return String(string).replace(/([ -])(?!$)/g, '$1?');
	  }
	
	  /**
	   * A bare-bones `Array#reduce` like utility function.
	   *
	   * @private
	   * @param {Array} array The array to iterate over.
	   * @param {Function} callback The function called per iteration.
	   * @returns {*} The accumulated result.
	   */
	  function reduce(array, callback) {
	    var accumulator = null;
	    each(array, function(value, index) {
	      accumulator = callback(accumulator, value, index, array);
	    });
	    return accumulator;
	  }
	
	  /**
	   * Removes leading and trailing whitespace from a string.
	   *
	   * @private
	   * @param {string} string The string to trim.
	   * @returns {string} The trimmed string.
	   */
	  function trim(string) {
	    return String(string).replace(/^ +| +$/g, '');
	  }
	
	  /*--------------------------------------------------------------------------*/
	
	  /**
	   * Creates a new platform object.
	   *
	   * @memberOf platform
	   * @param {Object|string} [ua=navigator.userAgent] The user agent string or
	   *  context object.
	   * @returns {Object} A platform object.
	   */
	  function parse(ua) {
	
	    /** The environment context object */
	    var context = root;
	
	    /** Used to flag when a custom context is provided */
	    var isCustomContext = ua && typeof ua == 'object' && getClassOf(ua) != 'String';
	
	    // juggle arguments
	    if (isCustomContext) {
	      context = ua;
	      ua = null;
	    }
	
	    /** Browser navigator object */
	    var nav = context.navigator || {};
	
	    /** Browser user agent string */
	    var userAgent = nav.userAgent || '';
	
	    ua || (ua = userAgent);
	
	    /** Used to flag when `thisBinding` is the [ModuleScope] */
	    var isModuleScope = isCustomContext || thisBinding == oldRoot;
	
	    /** Used to detect if browser is like Chrome */
	    var likeChrome = isCustomContext
	      ? !!nav.likeChrome
	      : /\bChrome\b/.test(ua) && !/internal|\n/i.test(toString.toString());
	
	    /** Internal `[[Class]]` value shortcuts */
	    var objectClass = 'Object',
	        airRuntimeClass = isCustomContext ? objectClass : 'ScriptBridgingProxyObject',
	        enviroClass = isCustomContext ? objectClass : 'Environment',
	        javaClass = (isCustomContext && context.java) ? 'JavaPackage' : getClassOf(context.java),
	        phantomClass = isCustomContext ? objectClass : 'RuntimeObject';
	
	    /** Detect Java environment */
	    var java = /\bJava/.test(javaClass) && context.java;
	
	    /** Detect Rhino */
	    var rhino = java && getClassOf(context.environment) == enviroClass;
	
	    /** A character to represent alpha */
	    var alpha = java ? 'a' : '\u03b1';
	
	    /** A character to represent beta */
	    var beta = java ? 'b' : '\u03b2';
	
	    /** Browser document object */
	    var doc = context.document || {};
	
	    /**
	     * Detect Opera browser (Presto-based)
	     * http://www.howtocreate.co.uk/operaStuff/operaObject.html
	     * http://dev.opera.com/articles/view/opera-mini-web-content-authoring-guidelines/#operamini
	     */
	    var opera = context.operamini || context.opera;
	
	    /** Opera `[[Class]]` */
	    var operaClass = reOpera.test(operaClass = (isCustomContext && opera) ? opera['[[Class]]'] : getClassOf(opera))
	      ? operaClass
	      : (opera = null);
	
	    /*------------------------------------------------------------------------*/
	
	    /** Temporary variable used over the script's lifetime */
	    var data;
	
	    /** The CPU architecture */
	    var arch = ua;
	
	    /** Platform description array */
	    var description = [];
	
	    /** Platform alpha/beta indicator */
	    var prerelease = null;
	
	    /** A flag to indicate that environment features should be used to resolve the platform */
	    var useFeatures = ua == userAgent;
	
	    /** The browser/environment version */
	    var version = useFeatures && opera && typeof opera.version == 'function' && opera.version();
	
	    /** A flag to indicate if the OS ends with "/ Version" */
	    var isSpecialCasedOS;
	
	    /* Detectable layout engines (order is important) */
	    var layout = getLayout([
	      'Trident',
	      { 'label': 'WebKit', 'pattern': 'AppleWebKit' },
	      'iCab',
	      'Presto',
	      'NetFront',
	      'Tasman',
	      'KHTML',
	      'Gecko'
	    ]);
	
	    /* Detectable browser names (order is important) */
	    var name = getName([
	      'Adobe AIR',
	      'Arora',
	      'Avant Browser',
	      'Breach',
	      'Camino',
	      'Epiphany',
	      'Fennec',
	      'Flock',
	      'Galeon',
	      'GreenBrowser',
	      'iCab',
	      'Iceweasel',
	      { 'label': 'SRWare Iron', 'pattern': 'Iron' },
	      'K-Meleon',
	      'Konqueror',
	      'Lunascape',
	      'Maxthon',
	      'Midori',
	      'Nook Browser',
	      'PhantomJS',
	      'Raven',
	      'Rekonq',
	      'RockMelt',
	      'SeaMonkey',
	      { 'label': 'Silk', 'pattern': '(?:Cloud9|Silk-Accelerated)' },
	      'Sleipnir',
	      'SlimBrowser',
	      'Sunrise',
	      'Swiftfox',
	      'WebPositive',
	      'Opera Mini',
	      { 'label': 'Opera Mini', 'pattern': 'OPiOS' },
	      'Opera',
	      { 'label': 'Opera', 'pattern': 'OPR' },
	      'Chrome',
	      { 'label': 'Chrome Mobile', 'pattern': '(?:CriOS|CrMo)' },
	      { 'label': 'Firefox', 'pattern': '(?:Firefox|Minefield)' },
	      { 'label': 'IE', 'pattern': 'IEMobile' },
	      { 'label': 'IE', 'pattern': 'MSIE' },
	      'Safari'
	    ]);
	
	    /* Detectable products (order is important) */
	    var product = getProduct([
	      { 'label': 'BlackBerry', 'pattern': 'BB10' },
	      'BlackBerry',
	      { 'label': 'Galaxy S', 'pattern': 'GT-I9000' },
	      { 'label': 'Galaxy S2', 'pattern': 'GT-I9100' },
	      { 'label': 'Galaxy S3', 'pattern': 'GT-I9300' },
	      { 'label': 'Galaxy S4', 'pattern': 'GT-I9500' },
	      'Google TV',
	      'Lumia',
	      'iPad',
	      'iPod',
	      'iPhone',
	      'Kindle',
	      { 'label': 'Kindle Fire', 'pattern': '(?:Cloud9|Silk-Accelerated)' },
	      'Nook',
	      'PlayBook',
	      'PlayStation 4',
	      'PlayStation 3',
	      'PlayStation Vita',
	      'TouchPad',
	      'Transformer',
	      { 'label': 'Wii U', 'pattern': 'WiiU' },
	      'Wii',
	      'Xbox One',
	      { 'label': 'Xbox 360', 'pattern': 'Xbox' },
	      'Xoom'
	    ]);
	
	    /* Detectable manufacturers */
	    var manufacturer = getManufacturer({
	      'Apple': { 'iPad': 1, 'iPhone': 1, 'iPod': 1 },
	      'Amazon': { 'Kindle': 1, 'Kindle Fire': 1 },
	      'Asus': { 'Transformer': 1 },
	      'Barnes & Noble': { 'Nook': 1 },
	      'BlackBerry': { 'PlayBook': 1 },
	      'Google': { 'Google TV': 1 },
	      'HP': { 'TouchPad': 1 },
	      'HTC': {},
	      'LG': {},
	      'Microsoft': { 'Xbox': 1, 'Xbox One': 1 },
	      'Motorola': { 'Xoom': 1 },
	      'Nintendo': { 'Wii U': 1,  'Wii': 1 },
	      'Nokia': { 'Lumia': 1 },
	      'Samsung': { 'Galaxy S': 1, 'Galaxy S2': 1, 'Galaxy S3': 1, 'Galaxy S4': 1 },
	      'Sony': { 'PlayStation 4': 1, 'PlayStation 3': 1, 'PlayStation Vita': 1 }
	    });
	
	    /* Detectable OSes (order is important) */
	    var os = getOS([
	      'Windows Phone ',
	      'Android',
	      'CentOS',
	      'Debian',
	      'Fedora',
	      'FreeBSD',
	      'Gentoo',
	      'Haiku',
	      'Kubuntu',
	      'Linux Mint',
	      'Red Hat',
	      'SuSE',
	      'Ubuntu',
	      'Xubuntu',
	      'Cygwin',
	      'Symbian OS',
	      'hpwOS',
	      'webOS ',
	      'webOS',
	      'Tablet OS',
	      'Linux',
	      'Mac OS X',
	      'Macintosh',
	      'Mac',
	      'Windows 98;',
	      'Windows '
	    ]);
	
	    /*------------------------------------------------------------------------*/
	
	    /**
	     * Picks the layout engine from an array of guesses.
	     *
	     * @private
	     * @param {Array} guesses An array of guesses.
	     * @returns {null|string} The detected layout engine.
	     */
	    function getLayout(guesses) {
	      return reduce(guesses, function(result, guess) {
	        return result || RegExp('\\b' + (
	          guess.pattern || qualify(guess)
	        ) + '\\b', 'i').exec(ua) && (guess.label || guess);
	      });
	    }
	
	    /**
	     * Picks the manufacturer from an array of guesses.
	     *
	     * @private
	     * @param {Array} guesses An object of guesses.
	     * @returns {null|string} The detected manufacturer.
	     */
	    function getManufacturer(guesses) {
	      return reduce(guesses, function(result, value, key) {
	        // lookup the manufacturer by product or scan the UA for the manufacturer
	        return result || (
	          value[product] ||
	          value[0/*Opera 9.25 fix*/, /^[a-z]+(?: +[a-z]+\b)*/i.exec(product)] ||
	          RegExp('\\b' + qualify(key) + '(?:\\b|\\w*\\d)', 'i').exec(ua)
	        ) && key;
	      });
	    }
	
	    /**
	     * Picks the browser name from an array of guesses.
	     *
	     * @private
	     * @param {Array} guesses An array of guesses.
	     * @returns {null|string} The detected browser name.
	     */
	    function getName(guesses) {
	      return reduce(guesses, function(result, guess) {
	        return result || RegExp('\\b' + (
	          guess.pattern || qualify(guess)
	        ) + '\\b', 'i').exec(ua) && (guess.label || guess);
	      });
	    }
	
	    /**
	     * Picks the OS name from an array of guesses.
	     *
	     * @private
	     * @param {Array} guesses An array of guesses.
	     * @returns {null|string} The detected OS name.
	     */
	    function getOS(guesses) {
	      return reduce(guesses, function(result, guess) {
	        var pattern = guess.pattern || qualify(guess);
	        if (!result && (result =
	              RegExp('\\b' + pattern + '(?:/[\\d.]+|[ \\w.]*)', 'i').exec(ua)
	            )) {
	          result = cleanupOS(result, pattern, guess.label || guess);
	        }
	        return result;
	      });
	    }
	
	    /**
	     * Picks the product name from an array of guesses.
	     *
	     * @private
	     * @param {Array} guesses An array of guesses.
	     * @returns {null|string} The detected product name.
	     */
	    function getProduct(guesses) {
	      return reduce(guesses, function(result, guess) {
	        var pattern = guess.pattern || qualify(guess);
	        if (!result && (result =
	              RegExp('\\b' + pattern + ' *\\d+[.\\w_]*', 'i').exec(ua) ||
	              RegExp('\\b' + pattern + '(?:; *(?:[a-z]+[_-])?[a-z]+\\d+|[^ ();-]*)', 'i').exec(ua)
	            )) {
	          // split by forward slash and append product version if needed
	          if ((result = String((guess.label && !RegExp(pattern, 'i').test(guess.label)) ? guess.label : result).split('/'))[1] && !/[\d.]+/.test(result[0])) {
	            result[0] += ' ' + result[1];
	          }
	          // correct character case and cleanup
	          guess = guess.label || guess;
	          result = format(result[0]
	            .replace(RegExp(pattern, 'i'), guess)
	            .replace(RegExp('; *(?:' + guess + '[_-])?', 'i'), ' ')
	            .replace(RegExp('(' + guess + ')[-_.]?(\\w)', 'i'), '$1 $2'));
	        }
	        return result;
	      });
	    }
	
	    /**
	     * Resolves the version using an array of UA patterns.
	     *
	     * @private
	     * @param {Array} patterns An array of UA patterns.
	     * @returns {null|string} The detected version.
	     */
	    function getVersion(patterns) {
	      return reduce(patterns, function(result, pattern) {
	        return result || (RegExp(pattern +
	          '(?:-[\\d.]+/|(?: for [\\w-]+)?[ /-])([\\d.]+[^ ();/_-]*)', 'i').exec(ua) || 0)[1] || null;
	      });
	    }
	
	    /**
	     * Returns `platform.description` when the platform object is coerced to a string.
	     *
	     * @name toString
	     * @memberOf platform
	     * @returns {string} Returns `platform.description` if available, else an empty string.
	     */
	    function toStringPlatform() {
	      return this.description || '';
	    }
	
	    /*------------------------------------------------------------------------*/
	
	    // convert layout to an array so we can add extra details
	    layout && (layout = [layout]);
	
	    // detect product names that contain their manufacturer's name
	    if (manufacturer && !product) {
	      product = getProduct([manufacturer]);
	    }
	    // clean up Google TV
	    if ((data = /\bGoogle TV\b/.exec(product))) {
	      product = data[0];
	    }
	    // detect simulators
	    if (/\bSimulator\b/i.test(ua)) {
	      product = (product ? product + ' ' : '') + 'Simulator';
	    }
	    // detect Opera Mini 8+ running in Turbo/Uncompressed mode on iOS
	    if (name == 'Opera Mini' && /\bOPiOS\b/.test(ua)) {
	      description.push('running in Turbo/Uncompressed mode');
	    }
	    // detect iOS
	    if (/^iP/.test(product)) {
	      name || (name = 'Safari');
	      os = 'iOS' + ((data = / OS ([\d_]+)/i.exec(ua))
	        ? ' ' + data[1].replace(/_/g, '.')
	        : '');
	    }
	    // detect Kubuntu
	    else if (name == 'Konqueror' && !/buntu/i.test(os)) {
	      os = 'Kubuntu';
	    }
	    // detect Android browsers
	    else if (manufacturer && manufacturer != 'Google' &&
	        ((/Chrome/.test(name) && !/\bMobile Safari\b/i.test(ua)) || /\bVita\b/.test(product))) {
	      name = 'Android Browser';
	      os = /\bAndroid\b/.test(os) ? os : 'Android';
	    }
	    // detect false positives for Firefox/Safari
	    else if (!name || (data = !/\bMinefield\b|\(Android;/i.test(ua) && /\b(?:Firefox|Safari)\b/.exec(name))) {
	      // escape the `/` for Firefox 1
	      if (name && !product && /[\/,]|^[^(]+?\)/.test(ua.slice(ua.indexOf(data + '/') + 8))) {
	        // clear name of false positives
	        name = null;
	      }
	      // reassign a generic name
	      if ((data = product || manufacturer || os) &&
	          (product || manufacturer || /\b(?:Android|Symbian OS|Tablet OS|webOS)\b/.test(os))) {
	        name = /[a-z]+(?: Hat)?/i.exec(/\bAndroid\b/.test(os) ? os : data) + ' Browser';
	      }
	    }
	    // detect Firefox OS
	    if ((data = /\((Mobile|Tablet).*?Firefox\b/i.exec(ua)) && data[1]) {
	      os = 'Firefox OS';
	      if (!product) {
	        product = data[1];
	      }
	    }
	    // detect non-Opera versions (order is important)
	    if (!version) {
	      version = getVersion([
	        '(?:Cloud9|CriOS|CrMo|IEMobile|Iron|Opera ?Mini|OPiOS|OPR|Raven|Silk(?!/[\\d.]+$))',
	        'Version',
	        qualify(name),
	        '(?:Firefox|Minefield|NetFront)'
	      ]);
	    }
	    // detect stubborn layout engines
	    if (layout == 'iCab' && parseFloat(version) > 3) {
	      layout = ['WebKit'];
	    } else if (
	        layout != 'Trident' &&
	        (data =
	          /\bOpera\b/.test(name) && (/\bOPR\b/.test(ua) ? 'Blink' : 'Presto') ||
	          /\b(?:Midori|Nook|Safari)\b/i.test(ua) && 'WebKit' ||
	          !layout && /\bMSIE\b/i.test(ua) && (os == 'Mac OS' ? 'Tasman' : 'Trident')
	        )
	    ) {
	      layout = [data];
	    }
	    // detect NetFront on PlayStation
	    else if (/\bPlayStation\b(?! Vita\b)/i.test(name) && layout == 'WebKit') {
	      layout = ['NetFront'];
	    }
	    // detect Windows Phone 7 desktop mode
	    if (name == 'IE' && (data = (/; *(?:XBLWP|ZuneWP)(\d+)/i.exec(ua) || 0)[1])) {
	      name += ' Mobile';
	      os = 'Windows Phone ' + (/\+$/.test(data) ? data : data + '.x');
	      description.unshift('desktop mode');
	    }
	    // detect Windows Phone 8+ desktop mode
	    else if (/\bWPDesktop\b/i.test(ua)) {
	      name = 'IE Mobile';
	      os = 'Windows Phone 8+';
	      description.unshift('desktop mode');
	      version || (version = (/\brv:([\d.]+)/.exec(ua) || 0)[1]);
	    }
	    // detect IE 11 and above
	    else if (name != 'IE' && layout == 'Trident' && (data = /\brv:([\d.]+)/.exec(ua))) {
	      if (!/\bWPDesktop\b/i.test(ua)) {
	        if (name) {
	          description.push('identifying as ' + name + (version ? ' ' + version : ''));
	        }
	        name = 'IE';
	      }
	      version = data[1];
	    }
	    // detect Microsoft Edge
	    else if ((name == 'Chrome' || name != 'IE') && (data = /\bEdge\/([\d.]+)/.exec(ua))) {
	      name = 'Microsoft Edge';
	      version = data[1];
	      layout = ['Trident'];
	    }
	    // leverage environment features
	    if (useFeatures) {
	      // detect server-side environments
	      // Rhino has a global function while others have a global object
	      if (isHostType(context, 'global')) {
	        if (java) {
	          data = java.lang.System;
	          arch = data.getProperty('os.arch');
	          os = os || data.getProperty('os.name') + ' ' + data.getProperty('os.version');
	        }
	        if (isModuleScope && isHostType(context, 'system') && (data = [context.system])[0]) {
	          os || (os = data[0].os || null);
	          try {
	            data[1] = context.require('ringo/engine').version;
	            version = data[1].join('.');
	            name = 'RingoJS';
	          } catch(e) {
	            if (data[0].global.system == context.system) {
	              name = 'Narwhal';
	            }
	          }
	        }
	        else if (typeof context.process == 'object' && (data = context.process)) {
	          name = 'Node.js';
	          arch = data.arch;
	          os = data.platform;
	          version = /[\d.]+/.exec(data.version)[0];
	        }
	        else if (rhino) {
	          name = 'Rhino';
	        }
	      }
	      // detect Adobe AIR
	      else if (getClassOf((data = context.runtime)) == airRuntimeClass) {
	        name = 'Adobe AIR';
	        os = data.flash.system.Capabilities.os;
	      }
	      // detect PhantomJS
	      else if (getClassOf((data = context.phantom)) == phantomClass) {
	        name = 'PhantomJS';
	        version = (data = data.version || null) && (data.major + '.' + data.minor + '.' + data.patch);
	      }
	      // detect IE compatibility modes
	      else if (typeof doc.documentMode == 'number' && (data = /\bTrident\/(\d+)/i.exec(ua))) {
	        // we're in compatibility mode when the Trident version + 4 doesn't
	        // equal the document mode
	        version = [version, doc.documentMode];
	        if ((data = +data[1] + 4) != version[1]) {
	          description.push('IE ' + version[1] + ' mode');
	          layout && (layout[1] = '');
	          version[1] = data;
	        }
	        version = name == 'IE' ? String(version[1].toFixed(1)) : version[0];
	      }
	      os = os && format(os);
	    }
	    // detect prerelease phases
	    if (version && (data =
	          /(?:[ab]|dp|pre|[ab]\d+pre)(?:\d+\+?)?$/i.exec(version) ||
	          /(?:alpha|beta)(?: ?\d)?/i.exec(ua + ';' + (useFeatures && nav.appMinorVersion)) ||
	          /\bMinefield\b/i.test(ua) && 'a'
	        )) {
	      prerelease = /b/i.test(data) ? 'beta' : 'alpha';
	      version = version.replace(RegExp(data + '\\+?$'), '') +
	        (prerelease == 'beta' ? beta : alpha) + (/\d+\+?/.exec(data) || '');
	    }
	    // detect Firefox Mobile
	    if (name == 'Fennec' || name == 'Firefox' && /\b(?:Android|Firefox OS)\b/.test(os)) {
	      name = 'Firefox Mobile';
	    }
	    // obscure Maxthon's unreliable version
	    else if (name == 'Maxthon' && version) {
	      version = version.replace(/\.[\d.]+/, '.x');
	    }
	    // detect Silk desktop/accelerated modes
	    else if (name == 'Silk') {
	      if (!/\bMobi/i.test(ua)) {
	        os = 'Android';
	        description.unshift('desktop mode');
	      }
	      if (/Accelerated *= *true/i.test(ua)) {
	        description.unshift('accelerated');
	      }
	    }
	    // detect Xbox 360 and Xbox One
	    else if (/\bXbox\b/i.test(product)) {
	      os = null;
	      if (product == 'Xbox 360' && /\bIEMobile\b/.test(ua)) {
	        description.unshift('mobile mode');
	      }
	    }
	    // add mobile postfix
	    else if ((/^(?:Chrome|IE|Opera)$/.test(name) || name && !product && !/Browser|Mobi/.test(name)) &&
	        (os == 'Windows CE' || /Mobi/i.test(ua))) {
	      name += ' Mobile';
	    }
	    // detect IE platform preview
	    else if (name == 'IE' && useFeatures && context.external === null) {
	      description.unshift('platform preview');
	    }
	    // detect BlackBerry OS version
	    // http://docs.blackberry.com/en/developers/deliverables/18169/HTTP_headers_sent_by_BB_Browser_1234911_11.jsp
	    else if ((/\bBlackBerry\b/.test(product) || /\bBB10\b/.test(ua)) && (data =
	          (RegExp(product.replace(/ +/g, ' *') + '/([.\\d]+)', 'i').exec(ua) || 0)[1] ||
	          version
	        )) {
	      data = [data, /BB10/.test(ua)];
	      os = (data[1] ? (product = null, manufacturer = 'BlackBerry') : 'Device Software') + ' ' + data[0];
	      version = null;
	    }
	    // detect Opera identifying/masking itself as another browser
	    // http://www.opera.com/support/kb/view/843/
	    else if (this != forOwn && (
	          product != 'Wii' && (
	            (useFeatures && opera) ||
	            (/Opera/.test(name) && /\b(?:MSIE|Firefox)\b/i.test(ua)) ||
	            (name == 'Firefox' && /\bOS X (?:\d+\.){2,}/.test(os)) ||
	            (name == 'IE' && (
	              (os && !/^Win/.test(os) && version > 5.5) ||
	              /\bWindows XP\b/.test(os) && version > 8 ||
	              version == 8 && !/\bTrident\b/.test(ua)
	            ))
	          )
	        ) && !reOpera.test((data = parse.call(forOwn, ua.replace(reOpera, '') + ';'))) && data.name) {
	
	      // when "indentifying", the UA contains both Opera and the other browser's name
	      data = 'ing as ' + data.name + ((data = data.version) ? ' ' + data : '');
	      if (reOpera.test(name)) {
	        if (/\bIE\b/.test(data) && os == 'Mac OS') {
	          os = null;
	        }
	        data = 'identify' + data;
	      }
	      // when "masking", the UA contains only the other browser's name
	      else {
	        data = 'mask' + data;
	        if (operaClass) {
	          name = format(operaClass.replace(/([a-z])([A-Z])/g, '$1 $2'));
	        } else {
	          name = 'Opera';
	        }
	        if (/\bIE\b/.test(data)) {
	          os = null;
	        }
	        if (!useFeatures) {
	          version = null;
	        }
	      }
	      layout = ['Presto'];
	      description.push(data);
	    }
	    // detect WebKit Nightly and approximate Chrome/Safari versions
	    if ((data = (/\bAppleWebKit\/([\d.]+\+?)/i.exec(ua) || 0)[1])) {
	      // correct build for numeric comparison
	      // (e.g. "532.5" becomes "532.05")
	      data = [parseFloat(data.replace(/\.(\d)$/, '.0$1')), data];
	      // nightly builds are postfixed with a `+`
	      if (name == 'Safari' && data[1].slice(-1) == '+') {
	        name = 'WebKit Nightly';
	        prerelease = 'alpha';
	        version = data[1].slice(0, -1);
	      }
	      // clear incorrect browser versions
	      else if (version == data[1] ||
	          version == (data[2] = (/\bSafari\/([\d.]+\+?)/i.exec(ua) || 0)[1])) {
	        version = null;
	      }
	      // use the full Chrome version when available
	      data[1] = (/\bChrome\/([\d.]+)/i.exec(ua) || 0)[1];
	      // detect Blink layout engine
	      if (data[0] == 537.36 && data[2] == 537.36 && parseFloat(data[1]) >= 28 && name != 'IE' && name != 'Microsoft Edge') {
	        layout = ['Blink'];
	      }
	      // detect JavaScriptCore
	      // http://stackoverflow.com/questions/6768474/how-can-i-detect-which-javascript-engine-v8-or-jsc-is-used-at-runtime-in-androi
	      if (!useFeatures || (!likeChrome && !data[1])) {
	        layout && (layout[1] = 'like Safari');
	        data = (data = data[0], data < 400 ? 1 : data < 500 ? 2 : data < 526 ? 3 : data < 533 ? 4 : data < 534 ? '4+' : data < 535 ? 5 : data < 537 ? 6 : data < 538 ? 7 : data < 601 ? 8 : '8');
	      } else {
	        layout && (layout[1] = 'like Chrome');
	        data = data[1] || (data = data[0], data < 530 ? 1 : data < 532 ? 2 : data < 532.05 ? 3 : data < 533 ? 4 : data < 534.03 ? 5 : data < 534.07 ? 6 : data < 534.10 ? 7 : data < 534.13 ? 8 : data < 534.16 ? 9 : data < 534.24 ? 10 : data < 534.30 ? 11 : data < 535.01 ? 12 : data < 535.02 ? '13+' : data < 535.07 ? 15 : data < 535.11 ? 16 : data < 535.19 ? 17 : data < 536.05 ? 18 : data < 536.10 ? 19 : data < 537.01 ? 20 : data < 537.11 ? '21+' : data < 537.13 ? 23 : data < 537.18 ? 24 : data < 537.24 ? 25 : data < 537.36 ? 26 : layout != 'Blink' ? '27' : '28');
	      }
	      // add the postfix of ".x" or "+" for approximate versions
	      layout && (layout[1] += ' ' + (data += typeof data == 'number' ? '.x' : /[.+]/.test(data) ? '' : '+'));
	      // obscure version for some Safari 1-2 releases
	      if (name == 'Safari' && (!version || parseInt(version) > 45)) {
	        version = data;
	      }
	    }
	    // detect Opera desktop modes
	    if (name == 'Opera' &&  (data = /\bzbov|zvav$/.exec(os))) {
	      name += ' ';
	      description.unshift('desktop mode');
	      if (data == 'zvav') {
	        name += 'Mini';
	        version = null;
	      } else {
	        name += 'Mobile';
	      }
	      os = os.replace(RegExp(' *' + data + '$'), '');
	    }
	    // detect Chrome desktop mode
	    else if (name == 'Safari' && /\bChrome\b/.exec(layout && layout[1])) {
	      description.unshift('desktop mode');
	      name = 'Chrome Mobile';
	      version = null;
	
	      if (/\bOS X\b/.test(os)) {
	        manufacturer = 'Apple';
	        os = 'iOS 4.3+';
	      } else {
	        os = null;
	      }
	    }
	    // strip incorrect OS versions
	    if (version && version.indexOf((data = /[\d.]+$/.exec(os))) == 0 &&
	        ua.indexOf('/' + data + '-') > -1) {
	      os = trim(os.replace(data, ''));
	    }
	    // add layout engine
	    if (layout && !/\b(?:Avant|Nook)\b/.test(name) && (
	        /Browser|Lunascape|Maxthon/.test(name) ||
	        /^(?:Adobe|Arora|Breach|Midori|Opera|Phantom|Rekonq|Rock|Sleipnir|Web)/.test(name) && layout[1])) {
	      // don't add layout details to description if they are falsey
	      (data = layout[layout.length - 1]) && description.push(data);
	    }
	    // combine contextual information
	    if (description.length) {
	      description = ['(' + description.join('; ') + ')'];
	    }
	    // append manufacturer
	    if (manufacturer && product && product.indexOf(manufacturer) < 0) {
	      description.push('on ' + manufacturer);
	    }
	    // append product
	    if (product) {
	      description.push((/^on /.test(description[description.length -1]) ? '' : 'on ') + product);
	    }
	    // parse OS into an object
	    if (os) {
	      data = / ([\d.+]+)$/.exec(os);
	      isSpecialCasedOS = data && os.charAt(os.length - data[0].length - 1) == '/';
	      os = {
	        'architecture': 32,
	        'family': (data && !isSpecialCasedOS) ? os.replace(data[0], '') : os,
	        'version': data ? data[1] : null,
	        'toString': function() {
	          var version = this.version;
	          return this.family + ((version && !isSpecialCasedOS) ? ' ' + version : '') + (this.architecture == 64 ? ' 64-bit' : '');
	        }
	      };
	    }
	    // add browser/OS architecture
	    if ((data = /\b(?:AMD|IA|Win|WOW|x86_|x)64\b/i.exec(arch)) && !/\bi686\b/i.test(arch)) {
	      if (os) {
	        os.architecture = 64;
	        os.family = os.family.replace(RegExp(' *' + data), '');
	      }
	      if (
	          name && (/\bWOW64\b/i.test(ua) ||
	          (useFeatures && /\w(?:86|32)$/.test(nav.cpuClass || nav.platform) && !/\bWin64; x64\b/i.test(ua)))
	      ) {
	        description.unshift('32-bit');
	      }
	    }
	
	    ua || (ua = null);
	
	    /*------------------------------------------------------------------------*/
	
	    /**
	     * The platform object.
	     *
	     * @name platform
	     * @type Object
	     */
	    var platform = {};
	
	    /**
	     * The platform description.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.description = ua;
	
	    /**
	     * The name of the browser's layout engine.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.layout = layout && layout[0];
	
	    /**
	     * The name of the product's manufacturer.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.manufacturer = manufacturer;
	
	    /**
	     * The name of the browser/environment.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.name = name;
	
	    /**
	     * The alpha/beta release indicator.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.prerelease = prerelease;
	
	    /**
	     * The name of the product hosting the browser.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.product = product;
	
	    /**
	     * The browser's user agent string.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.ua = ua;
	
	    /**
	     * The browser/environment version.
	     *
	     * @memberOf platform
	     * @type string|null
	     */
	    platform.version = name && version;
	
	    /**
	     * The name of the operating system.
	     *
	     * @memberOf platform
	     * @type Object
	     */
	    platform.os = os || {
	
	      /**
	       * The CPU architecture the OS is built for.
	       *
	       * @memberOf platform.os
	       * @type number|null
	       */
	      'architecture': null,
	
	      /**
	       * The family of the OS.
	       *
	       * Common values include:
	       * "Windows", "Windows Server 2008 R2 / 7", "Windows Server 2008 / Vista",
	       * "Windows XP", "OS X", "Ubuntu", "Debian", "Fedora", "Red Hat", "SuSE",
	       * "Android", "iOS" and "Windows Phone"
	       *
	       * @memberOf platform.os
	       * @type string|null
	       */
	      'family': null,
	
	      /**
	       * The version of the OS.
	       *
	       * @memberOf platform.os
	       * @type string|null
	       */
	      'version': null,
	
	      /**
	       * Returns the OS string.
	       *
	       * @memberOf platform.os
	       * @returns {string} The OS string.
	       */
	      'toString': function() { return 'null'; }
	    };
	
	    platform.parse = parse;
	    platform.toString = toStringPlatform;
	
	    if (platform.version) {
	      description.unshift(version);
	    }
	    if (platform.name) {
	      description.unshift(name);
	    }
	    if (os && name && !(os == String(os).split(' ')[0] && (os == name.split(' ')[0] || product))) {
	      description.push(product ? '(' + os + ')' : 'on ' + os);
	    }
	    if (description.length) {
	      platform.description = description.join(' ');
	    }
	    return platform;
	  }
	
	  /*--------------------------------------------------------------------------*/
	
	  // export platform
	  // some AMD build optimizers, like r.js, check for condition patterns like the following:
	  if (true) {
	    // define as an anonymous module so, through path mapping, it can be aliased
	    !(__WEBPACK_AMD_DEFINE_RESULT__ = function() {
	      return parse();
	    }.call(exports, __webpack_require__, exports, module), __WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
	  }
	  // check for `exports` after `define` in case a build optimizer adds an `exports` object
	  else if (freeExports && freeModule) {
	    // in Narwhal, Node.js, Rhino -require, or RingoJS
	    forOwn(parse(), function(value, key) {
	      freeExports[key] = value;
	    });
	  }
	  // in a browser or Rhino
	  else {
	    root.platform = parse();
	  }
	}.call(this));
	
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(117)(module), (function() { return this; }())))

/***/ },
/* 115 */
/***/ function(module, exports, __webpack_require__) {

	// style-loader: Adds some css to the DOM by adding a <style> tag
	
	// load the styles
	var content = __webpack_require__(73);
	if(typeof content === 'string') content = [[module.id, content, '']];
	// add the styles to the DOM
	var update = __webpack_require__(38)(content, {});
	if(content.locals) module.exports = content.locals;
	// Hot Module Replacement
	if(false) {
		// When the styles change, update the <style> tags
		if(!content.locals) {
			module.hot.accept("!!./../css-loader/index.js!./../sass-loader/index.js!./_reset.scss", function() {
				var newContent = require("!!./../css-loader/index.js!./../sass-loader/index.js!./_reset.scss");
				if(typeof newContent === 'string') newContent = [[module.id, newContent, '']];
				update(newContent);
			});
		}
		// When the module is disposed, remove the <style> tags
		module.hot.dispose(function() { update(); });
	}

/***/ },
/* 116 */
/***/ function(module, exports, __webpack_require__) {

	// style-loader: Adds some css to the DOM by adding a <style> tag
	
	// load the styles
	var content = __webpack_require__(74);
	if(typeof content === 'string') content = [[module.id, content, '']];
	// add the styles to the DOM
	var update = __webpack_require__(38)(content, {});
	if(content.locals) module.exports = content.locals;
	// Hot Module Replacement
	if(false) {
		// When the styles change, update the <style> tags
		if(!content.locals) {
			module.hot.accept("!!./../../node_modules/css-loader/index.js!./../../node_modules/sass-loader/index.js!./index.sass", function() {
				var newContent = require("!!./../../node_modules/css-loader/index.js!./../../node_modules/sass-loader/index.js!./index.sass");
				if(typeof newContent === 'string') newContent = [[module.id, newContent, '']];
				update(newContent);
			});
		}
		// When the module is disposed, remove the <style> tags
		module.hot.dispose(function() { update(); });
	}

/***/ },
/* 117 */
/***/ function(module, exports) {

	module.exports = function(module) {
		if(!module.webpackPolyfill) {
			module.deprecate = function() {};
			module.paths = [];
			// module.parent = undefined by default
			module.children = [];
			module.webpackPolyfill = 1;
		}
		return module;
	}


/***/ }
/******/ ]);